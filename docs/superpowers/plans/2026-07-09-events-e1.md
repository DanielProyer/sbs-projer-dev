# Events-Modul Phase E1 — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Event-Jahr-Entität + Kontaktliste pro Event-Jahr (Rollen, Vorjahres-Übernahme) + WhatsApp/Anruf gemäss Spec `docs/superpowers/specs/2026-07-09-events-e1-design.md`.

**Architecture:** Zwei neue gesyncte Entities (`events`, `event_kontakte`) nach der bestehenden Kontakt-Vertikale (DTO → Isar-Local + Web-Stub + Export → Mapper → IsarService → Repository → Provider → Sync-Tier). Events referenzieren bestehende Veranstaltungs-Betriebe (`zapfsysteme` enthält exakt `'Veranstaltungen'`); Kontakte bleiben globale Personen, Rollen liegen auf der Zuordnung. **Abweichung vom Kontakt-Muster:** `EventRepository.create` generiert die Server-UUID client-seitig (`uuid` Package, v4) und setzt sie als `serverId` — damit haben `event_kontakte.eventId`-Referenzen auch bei offline angelegten Events sofort einen gültigen Wert.

**Tech Stack:** Flutter/Dart, Riverpod, Isar (Conditional Exports), Supabase (MCP `apply_migration`, Project `pltbaqqwpnmdajwgnhpd`), GoRouter, url_launcher, uuid.

**Projektregeln:**
- Shell: Git Bash, `export PATH="$PATH:/c/flutter/bin"`, App-Verzeichnis `sbs_projer_app`.
- Local-Imports IMMER über `*_export.dart`. Kommentare/Labels Deutsch.
- KEINE `?ausdruck`-Collection-Elemente (bricht build_runner — Lint dafür ist deaktiviert). Immer `if (x != null) …`.
- `.g.dart` ist gitignored — nie stagen.
- Nach jedem Task: `flutter analyze` ohne neue errors/warnings (Altbestand: 47 infos), dann committen.

---

### Task 1: Migration 119 — Tabellen `events` + `event_kontakte`

**Files:**
- Create: `Datenbank/migrations/119_events.sql`

- [ ] **Step 1: Migration schreiben**

```sql
-- ============================================================
-- Migration 119: Events-Modul E1 — events + event_kontakte
-- Projekt: SBS Projer App
-- Stand: 09.07.2026
-- ============================================================

CREATE TABLE events (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  betrieb_id uuid NOT NULL REFERENCES betriebe(id) ON DELETE CASCADE,
  jahr int NOT NULL,
  termin_von date,
  termin_bis date,
  notizen text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now(),
  UNIQUE (user_id, betrieb_id, jahr)
);

ALTER TABLE events ENABLE ROW LEVEL SECURITY;
CREATE POLICY events_user_policy ON events
  FOR ALL USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
CREATE TRIGGER update_events_updated_at BEFORE UPDATE ON events
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE INDEX idx_events_user_jahr ON events(user_id, jahr);

CREATE TABLE event_kontakte (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  event_id uuid NOT NULL REFERENCES events(id) ON DELETE CASCADE,
  kontakt_id uuid NOT NULL REFERENCES kontakte(id) ON DELETE CASCADE,
  rolle text NOT NULL CHECK (rolle IN (
    'ok', 'bau', 'stand', 'event_heineken', 'rsl', 'monteur', 'stardrinks', 'sonstige'
  )),
  bemerkung text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now(),
  UNIQUE (event_id, kontakt_id, rolle)
);

ALTER TABLE event_kontakte ENABLE ROW LEVEL SECURITY;
CREATE POLICY event_kontakte_user_policy ON event_kontakte
  FOR ALL USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
CREATE TRIGGER update_event_kontakte_updated_at BEFORE UPDATE ON event_kontakte
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE INDEX idx_event_kontakte_event ON event_kontakte(user_id, event_id);
```

- [ ] **Step 2: Migration anwenden**

Via Supabase MCP `apply_migration` (name `119_events`, Project `pltbaqqwpnmdajwgnhpd`).
Verifizieren mit `execute_sql`: `SELECT table_name FROM information_schema.tables WHERE table_name IN ('events','event_kontakte');` → 2 Zeilen.

- [ ] **Step 3: Commit**

```bash
git add Datenbank/migrations/119_events.sql
git commit -m "feat(events): Migration 119 events + event_kontakte (E1)"
```

---

### Task 2: DTOs `Event` + `EventKontakt`

**Files:**
- Create: `sbs_projer_app/lib/data/models/event.dart`
- Create: `sbs_projer_app/lib/data/models/event_kontakt.dart`

- [ ] **Step 1: `event.dart` erstellen**

```dart
/// Supabase-DTO für ein Event-Jahr (z. B. «Lumnezia 2025»).
/// Referenziert den Veranstaltungs-Betrieb (zapfsysteme enthält 'Veranstaltungen').
class Event {
  final String id;
  final String userId;
  final String betriebId;
  final int jahr;
  final DateTime? terminVon;
  final DateTime? terminBis;
  final String? notizen;
  final DateTime? createdAt;
  final DateTime? updatedAt;

  Event({
    required this.id,
    required this.userId,
    required this.betriebId,
    required this.jahr,
    this.terminVon,
    this.terminBis,
    this.notizen,
    this.createdAt,
    this.updatedAt,
  });

  factory Event.fromJson(Map<String, dynamic> json) {
    return Event(
      id: json['id'],
      userId: json['user_id'],
      betriebId: json['betrieb_id'],
      jahr: json['jahr'],
      terminVon: json['termin_von'] != null ? DateTime.parse(json['termin_von']) : null,
      terminBis: json['termin_bis'] != null ? DateTime.parse(json['termin_bis']) : null,
      notizen: json['notizen'],
      createdAt: json['created_at'] != null ? DateTime.parse(json['created_at']) : null,
      updatedAt: json['updated_at'] != null ? DateTime.parse(json['updated_at']) : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'user_id': userId,
      'betrieb_id': betriebId,
      'jahr': jahr,
      'termin_von': terminVon?.toIso8601String().split('T').first,
      'termin_bis': terminBis?.toIso8601String().split('T').first,
      'notizen': notizen,
    };
  }
}
```

- [ ] **Step 2: `event_kontakt.dart` erstellen**

```dart
/// Supabase-DTO für die Zuordnung Kontakt ↔ Event-Jahr (Rolle pro Zuordnung).
class EventKontakt {
  final String id;
  final String userId;
  final String eventId;
  final String kontaktId;
  final String rolle;
  final String? bemerkung;
  final DateTime? createdAt;
  final DateTime? updatedAt;

  EventKontakt({
    required this.id,
    required this.userId,
    required this.eventId,
    required this.kontaktId,
    required this.rolle,
    this.bemerkung,
    this.createdAt,
    this.updatedAt,
  });

  factory EventKontakt.fromJson(Map<String, dynamic> json) {
    return EventKontakt(
      id: json['id'],
      userId: json['user_id'],
      eventId: json['event_id'],
      kontaktId: json['kontakt_id'],
      rolle: json['rolle'],
      bemerkung: json['bemerkung'],
      createdAt: json['created_at'] != null ? DateTime.parse(json['created_at']) : null,
      updatedAt: json['updated_at'] != null ? DateTime.parse(json['updated_at']) : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'user_id': userId,
      'event_id': eventId,
      'kontakt_id': kontaktId,
      'rolle': rolle,
      'bemerkung': bemerkung,
    };
  }

  /// Rollen in fester Gruppierungs-Reihenfolge für die Anzeige.
  static const rollenReihenfolge = [
    'event_heineken', 'rsl', 'ok', 'bau', 'stand', 'monteur', 'stardrinks', 'sonstige',
  ];

  static String rolleLabel(String rolle) => switch (rolle) {
        'event_heineken' => 'Eventverantwortlicher',
        'rsl' => 'RSL',
        'ok' => 'OK',
        'bau' => 'Bau',
        'stand' => 'Stand',
        'monteur' => 'Monteur',
        'stardrinks' => 'Stardrinks',
        'sonstige' => 'Sonstige',
        _ => rolle,
      };
}
```

- [ ] **Step 3: Analyze + Commit**

Run: `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && flutter analyze`
Expected: keine neuen Findings.

```bash
git add sbs_projer_app/lib/data/models/event.dart sbs_projer_app/lib/data/models/event_kontakt.dart
git commit -m "feat(events): DTOs Event + EventKontakt (E1)"
```

---

### Task 3: Isar-Locals, Web-Stubs, Exports, IsarService

**Files:**
- Create: `sbs_projer_app/lib/data/local/event_local.dart`
- Create: `sbs_projer_app/lib/data/local/event_kontakt_local.dart`
- Create: `sbs_projer_app/lib/data/local/event_local_export.dart`
- Create: `sbs_projer_app/lib/data/local/event_kontakt_local_export.dart`
- Create: `sbs_projer_app/lib/data/local/web/event_local_web.dart`
- Create: `sbs_projer_app/lib/data/local/web/event_kontakt_local_web.dart`
- Modify: `sbs_projer_app/lib/services/storage/isar_service.dart` (Schema-Liste Z. 36–56 + neue Methoden)
- Modify: `sbs_projer_app/lib/services/storage/isar_service_web.dart` (Stubs)

- [ ] **Step 1: `event_local.dart`**

```dart
import 'package:flutter/foundation.dart' show kIsWeb;
import 'package:isar/isar.dart';

part 'event_local.g.dart';

@collection
class EventLocal {
  Id id = Isar.autoIncrement;

  @ignore
  String get routeId => kIsWeb ? serverId! : id.toString();

  // Supabase Sync
  @Index()
  String? serverId;
  @Index()
  bool isSynced = false;
  DateTime? lastModifiedAt;

  // Felder
  late String userId;
  late String betriebId;
  late int jahr;
  DateTime? terminVon;
  DateTime? terminBis;
  String? notizen;
  DateTime? createdAt;
  DateTime? updatedAt;
}
```

- [ ] **Step 2: `event_kontakt_local.dart`**

```dart
import 'package:flutter/foundation.dart' show kIsWeb;
import 'package:isar/isar.dart';

part 'event_kontakt_local.g.dart';

@collection
class EventKontaktLocal {
  Id id = Isar.autoIncrement;

  @ignore
  String get routeId => kIsWeb ? serverId! : id.toString();

  // Supabase Sync
  @Index()
  String? serverId;
  @Index()
  bool isSynced = false;
  DateTime? lastModifiedAt;

  // Felder
  late String userId;
  @Index()
  late String eventId;
  late String kontaktId;
  late String rolle;
  String? bemerkung;
  DateTime? createdAt;
  DateTime? updatedAt;
}
```

- [ ] **Step 3: Web-Stubs**

`web/event_local_web.dart` (Plain-Klasse, kein Isar, keine `late`):

```dart
/// Web-Stub für EventLocal (kein Isar auf Web).
class EventLocal {
  int id = 0;

  String get routeId => serverId!;

  // Supabase Sync
  String? serverId;
  bool isSynced = false;
  DateTime? lastModifiedAt;

  // Felder
  String userId = '';
  String betriebId = '';
  int jahr = 0;
  DateTime? terminVon;
  DateTime? terminBis;
  String? notizen;
  DateTime? createdAt;
  DateTime? updatedAt;
}
```

`web/event_kontakt_local_web.dart` analog (`eventId = ''`, `kontaktId = ''`, `rolle = ''`, `bemerkung`, Sync-Felder identisch).

- [ ] **Step 4: Conditional Exports**

`event_local_export.dart`:
```dart
export 'event_local.dart' if (dart.library.html) 'web/event_local_web.dart';
```
`event_kontakt_local_export.dart`:
```dart
export 'event_kontakt_local.dart' if (dart.library.html) 'web/event_kontakt_local_web.dart';
```

- [ ] **Step 5: IsarService erweitern**

In `isar_service.dart`: Imports für beide Locals (direkt, NICHT via export — hier ist Isar nativ), `EventLocalSchema` + `EventKontaktLocalSchema` in die Schema-Liste (Z. 36–56), dann neue Methoden nach dem Kontakt-Block (Z. 248):

```dart
  // ─── Event ───
  static Future<List<EventLocal>> eventFindAll() =>
      instance.eventLocals.where().findAll();
  static Future<EventLocal?> eventFindByServerId(String serverId) =>
      instance.eventLocals.filter().serverIdEqualTo(serverId).findFirst();
  static Future<void> eventPut(EventLocal e) =>
      instance.writeTxn(() => instance.eventLocals.put(e));
  static Future<void> eventDelete(int id) =>
      instance.writeTxn(() => instance.eventLocals.delete(id));

  // ─── EventKontakt ───
  static Future<List<EventKontaktLocal>> eventKontaktFindByEvent(String eventId) =>
      instance.eventKontaktLocals.filter().eventIdEqualTo(eventId).findAll();
  static Future<EventKontaktLocal?> eventKontaktFindByServerId(String serverId) =>
      instance.eventKontaktLocals.filter().serverIdEqualTo(serverId).findFirst();
  static Future<void> eventKontaktPut(EventKontaktLocal e) =>
      instance.writeTxn(() => instance.eventKontaktLocals.put(e));
  static Future<void> eventKontaktDelete(int id) =>
      instance.writeTxn(() => instance.eventKontaktLocals.delete(id));
```

In `isar_service_web.dart` analog zum Kontakt-Block (Z. 133–141) 8 `UnsupportedError`-Stubs mit denselben Methodennamen ergänzen.

- [ ] **Step 6: build_runner + Analyze**

Run: `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && dart run build_runner build --delete-conflicting-outputs` (Timeout 600000)
Expected: `Succeeded` (event_local.g.dart + event_kontakt_local.g.dart erzeugt).
Run: `flutter analyze` — keine neuen Findings.

- [ ] **Step 7: Commit** (`.g.dart` NICHT stagen!)

```bash
git add sbs_projer_app/lib/data/local/event_local.dart sbs_projer_app/lib/data/local/event_kontakt_local.dart sbs_projer_app/lib/data/local/event_local_export.dart sbs_projer_app/lib/data/local/event_kontakt_local_export.dart sbs_projer_app/lib/data/local/web/event_local_web.dart sbs_projer_app/lib/data/local/web/event_kontakt_local_web.dart sbs_projer_app/lib/services/storage/isar_service.dart sbs_projer_app/lib/services/storage/isar_service_web.dart
git commit -m "feat(events): Isar-Locals, Web-Stubs, IsarService fuer Event + EventKontakt (E1)"
```

---

### Task 4: Utils mit Tests (TDD) — Status, WhatsApp-Link, Vorjahres-Merge

**Files:**
- Create: `sbs_projer_app/lib/core/util/event_status.dart`
- Create: `sbs_projer_app/lib/core/util/whatsapp_link.dart`
- Test: `sbs_projer_app/test/core/util/event_status_test.dart`
- Test: `sbs_projer_app/test/core/util/whatsapp_link_test.dart`

- [ ] **Step 1: Failing Tests schreiben**

`test/core/util/event_status_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:sbs_projer_app/core/util/event_status.dart';

void main() {
  final heute = DateTime(2026, 7, 15);

  group('eventStatus', () {
    test('ohne Termin: offen', () {
      expect(eventStatus(null, null, heute), EventStatus.offen);
    });
    test('vor Beginn: kommend', () {
      expect(eventStatus(DateTime(2026, 7, 20), DateTime(2026, 7, 22), heute),
          EventStatus.kommend);
    });
    test('waehrend (Randtage inklusive): laufend', () {
      expect(eventStatus(DateTime(2026, 7, 15), DateTime(2026, 7, 17), heute),
          EventStatus.laufend);
      expect(eventStatus(DateTime(2026, 7, 13), DateTime(2026, 7, 15), heute),
          EventStatus.laufend);
    });
    test('nach Ende: vorbei', () {
      expect(eventStatus(DateTime(2026, 7, 1), DateTime(2026, 7, 3), heute),
          EventStatus.vorbei);
    });
    test('nur von gesetzt: von zaehlt als ganzer Event-Tag', () {
      expect(eventStatus(DateTime(2026, 7, 15), null, heute), EventStatus.laufend);
      expect(eventStatus(DateTime(2026, 7, 10), null, heute), EventStatus.vorbei);
      expect(eventStatus(DateTime(2026, 7, 20), null, heute), EventStatus.kommend);
    });
  });
}
```

`test/core/util/whatsapp_link_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:sbs_projer_app/core/util/whatsapp_link.dart';

void main() {
  group('whatsappNummer', () {
    test('CH-Nummer mit fuehrender 0 -> 41…', () {
      expect(whatsappNummer('079 123 45 67'), '41791234567');
    });
    test('bereits international mit +', () {
      expect(whatsappNummer('+41 79 123 45 67'), '41791234567');
    });
    test('00-Praefix', () {
      expect(whatsappNummer('0049 171 1234567'), '491711234567');
    });
    test('Sonderzeichen werden entfernt', () {
      expect(whatsappNummer('079/123-45-67'), '41791234567');
    });
    test('null oder zu kurz -> null', () {
      expect(whatsappNummer(null), isNull);
      expect(whatsappNummer('123'), isNull);
    });
  });

  group('whatsappUri', () {
    test('baut wa.me-Link', () {
      expect(whatsappUri('079 123 45 67').toString(), 'https://wa.me/41791234567');
    });
  });
}
```

- [ ] **Step 2: Tests ausführen — MÜSSEN fehlschlagen**

Run: `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && flutter test test/core/util/event_status_test.dart test/core/util/whatsapp_link_test.dart`
Expected: FAIL (Dateien existieren nicht).

- [ ] **Step 3: Implementieren**

`lib/core/util/event_status.dart`:

```dart
/// Status eines Event-Jahres, abgeleitet aus dem Termin (kein DB-Feld).
enum EventStatus { offen, kommend, laufend, vorbei }

/// [von]/[bis] sind Datums-Werte (ohne Zeit); Randtage zählen als laufend.
/// Ohne Termin: offen. Nur [von] gesetzt: eintägiges Event.
EventStatus eventStatus(DateTime? von, DateTime? bis, DateTime heute) {
  if (von == null && bis == null) return EventStatus.offen;
  final h = DateTime(heute.year, heute.month, heute.day);
  final start = von ?? bis!;
  final ende = bis ?? von!;
  if (h.isBefore(DateTime(start.year, start.month, start.day))) {
    return EventStatus.kommend;
  }
  if (h.isAfter(DateTime(ende.year, ende.month, ende.day))) {
    return EventStatus.vorbei;
  }
  return EventStatus.laufend;
}
```

`lib/core/util/whatsapp_link.dart`:

```dart
/// Baut aus einer rohen Telefonnummer die wa.me-Nummer (E.164 ohne '+').
/// CH-Default: führende 0 wird zu 41. Zu kurze/leere Nummern -> null.
String? whatsappNummer(String? telefon) {
  if (telefon == null) return null;
  var d = telefon.replaceAll(RegExp(r'[^\d+]'), '');
  if (d.startsWith('+')) {
    d = d.substring(1);
  } else if (d.startsWith('00')) {
    d = d.substring(2);
  } else if (d.startsWith('0')) {
    d = '41${d.substring(1)}';
  }
  if (d.length < 8) return null;
  return d;
}

/// wa.me-Link für [telefon]; wirft, wenn die Nummer unbrauchbar ist —
/// vorher mit [whatsappNummer] prüfen.
Uri whatsappUri(String telefon) => Uri.parse('https://wa.me/${whatsappNummer(telefon)!}');
```

- [ ] **Step 4: Tests grün + Commit**

Run: `flutter test test/core/util/event_status_test.dart test/core/util/whatsapp_link_test.dart`
Expected: `All tests passed!`

```bash
git add sbs_projer_app/lib/core/util/event_status.dart sbs_projer_app/lib/core/util/whatsapp_link.dart sbs_projer_app/test/core/util/event_status_test.dart sbs_projer_app/test/core/util/whatsapp_link_test.dart
git commit -m "feat(events): Status- und WhatsApp-Utils inkl. Tests (E1, TDD)"
```

---

### Task 5: Mapper + Repositories + Provider + Vorjahres-Übernahme

**Files:**
- Create: `sbs_projer_app/lib/data/mappers/event_mapper.dart`
- Create: `sbs_projer_app/lib/data/mappers/event_kontakt_mapper.dart`
- Create: `sbs_projer_app/lib/data/repositories/event_repository.dart`
- Create: `sbs_projer_app/lib/data/repositories/event_kontakt_repository.dart`
- Create: `sbs_projer_app/lib/presentation/providers/event_providers.dart`
- Test: `sbs_projer_app/test/data/event_vorjahr_test.dart`

- [ ] **Step 1: Mapper**

`event_mapper.dart`:

```dart
import 'package:sbs_projer_app/data/local/event_local_export.dart';
import 'package:sbs_projer_app/data/models/event.dart';

class EventMapper {
  static EventLocal fromDto(Event dto, {EventLocal? existing}) {
    final local = existing ?? EventLocal();
    local.serverId = dto.id;
    local.userId = dto.userId;
    local.betriebId = dto.betriebId;
    local.jahr = dto.jahr;
    local.terminVon = dto.terminVon;
    local.terminBis = dto.terminBis;
    local.notizen = dto.notizen;
    local.createdAt = dto.createdAt;
    local.updatedAt = dto.updatedAt;
    local.isSynced = true;
    local.lastModifiedAt = dto.updatedAt ?? dto.createdAt ?? DateTime.now();
    return local;
  }

  static Map<String, dynamic> toJson(EventLocal local) {
    final json = <String, dynamic>{
      'user_id': local.userId,
      'betrieb_id': local.betriebId,
      'jahr': local.jahr,
      'termin_von': local.terminVon?.toIso8601String().split('T').first,
      'termin_bis': local.terminBis?.toIso8601String().split('T').first,
      'notizen': local.notizen,
    };
    if (local.serverId != null) json['id'] = local.serverId;
    return json;
  }
}
```

`event_kontakt_mapper.dart` analog (Felder `eventId/event_id`, `kontaktId/kontakt_id`, `rolle`, `bemerkung`).

- [ ] **Step 2: EventRepository**

`event_repository.dart` — Muster `kontakt_repository.dart`, plus client-seitige UUID:

```dart
import 'package:flutter/foundation.dart' show kIsWeb;
import 'package:uuid/uuid.dart';
import 'package:sbs_projer_app/data/local/event_local_export.dart';
import 'package:sbs_projer_app/data/mappers/event_mapper.dart';
import 'package:sbs_projer_app/data/models/event.dart';
import 'package:sbs_projer_app/services/storage/isar_service.dart'
    if (dart.library.html) 'package:sbs_projer_app/services/storage/isar_service_web.dart';
import 'package:sbs_projer_app/services/supabase/supabase_service.dart';
```

(Import-Variante für IsarService exakt so übernehmen, wie `kontakt_repository.dart` sie macht — vorher dort nachsehen und identisch halten.)

Methoden:
- `getAll()`: Web = `from('events').select().eq('user_id', _userId).order('jahr', ascending: false)` → `EventMapper.fromDto(Event.fromJson(r))`; Native = `IsarService.eventFindAll()`.
- `getById(String id)`: Web = `.eq('id', id).maybeSingle()`; Native = `eventFindByServerId` mit Fallback `eventGet(int.parse(id))` analog Kontakt-Muster (`getById`).
- `save(EventLocal e)`: `e.userId = _userId; e.serverId ??= const Uuid().v4();` Web = upsert `EventMapper.toJson(e)`; Native = `isSynced=false; lastModifiedAt=now(); eventPut(e)`.
- `delete(String id)`: Web = delete eq id; Native = `eventDelete(int.parse(id))`. (In der DB räumt ON DELETE CASCADE die `event_kontakte` ab; die lokalen Isar-Zuordnungen entfernt der Aufrufer vorher explizit — siehe Task 9 Lösch-Ablauf.)
- `getVorjahr(String betriebId, int jahr)`: alle Events des Betriebs laden, grösstes `jahr < jahr` zurückgeben (oder null).

- [ ] **Step 3: EventKontaktRepository + Merge-Helfer**

`event_kontakt_repository.dart`:
- `getByEvent(String eventId)`: Web = `.eq('event_id', eventId)`; Native = `eventKontaktFindByEvent`.
- `save(EventKontaktLocal z)`: analog (`serverId ??= Uuid().v4()`).
- `delete(String id)` analog.
- `deleteByEvent(String eventId)`: Web = delete eq event_id; Native = Schleife über `eventKontaktFindByEvent` + delete.
- **Pure statische Merge-Funktion** (testbar):

```dart
  /// Liefert die Zuordnungen aus [quelle], deren (kontaktId, rolle)-Kombination
  /// in [ziel] noch fehlt (Vorjahres-Übernahme ohne Duplikate).
  static List<EventKontaktLocal> fehlendeZuordnungen(
      List<EventKontaktLocal> quelle, List<EventKontaktLocal> ziel) {
    return quelle
        .where((q) => !ziel.any(
            (z) => z.kontaktId == q.kontaktId && z.rolle == q.rolle))
        .toList();
  }

  /// Übernimmt Kontakte aus dem Vorjahres-Event [vonEventId] in [zuEventId].
  /// Rückgabe: Anzahl übernommener Zuordnungen.
  static Future<int> uebernehmeVon(String vonEventId, String zuEventId) async {
    final quelle = await getByEvent(vonEventId);
    final ziel = await getByEvent(zuEventId);
    final neu = fehlendeZuordnungen(quelle, ziel);
    for (final q in neu) {
      final z = EventKontaktLocal()
        ..eventId = zuEventId
        ..kontaktId = q.kontaktId
        ..rolle = q.rolle
        ..bemerkung = q.bemerkung;
      await save(z);
    }
    return neu.length;
  }
```

- [ ] **Step 4: Test für Merge-Regel (TDD-Reihenfolge: Test zuerst, Fail sehen, dann Step 3 fertigstellen)**

`test/data/event_vorjahr_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:sbs_projer_app/data/local/event_kontakt_local_export.dart';
import 'package:sbs_projer_app/data/repositories/event_kontakt_repository.dart';

EventKontaktLocal _z(String kontaktId, String rolle) => EventKontaktLocal()
  ..userId = 'test'
  ..eventId = 'evt'
  ..kontaktId = kontaktId
  ..rolle = rolle;

void main() {
  group('fehlendeZuordnungen', () {
    test('leeres Ziel: alles wird uebernommen', () {
      final quelle = [_z('k1', 'ok'), _z('k2', 'stand')];
      expect(EventKontaktRepository.fehlendeZuordnungen(quelle, []).length, 2);
    });
    test('gleiche (kontaktId, rolle) wird uebersprungen', () {
      final quelle = [_z('k1', 'ok'), _z('k2', 'stand')];
      final ziel = [_z('k1', 'ok')];
      final neu = EventKontaktRepository.fehlendeZuordnungen(quelle, ziel);
      expect(neu.length, 1);
      expect(neu.first.kontaktId, 'k2');
    });
    test('gleicher Kontakt mit ANDERER Rolle wird uebernommen', () {
      final quelle = [_z('k1', 'bau')];
      final ziel = [_z('k1', 'ok')];
      expect(EventKontaktRepository.fehlendeZuordnungen(quelle, ziel).length, 1);
    });
  });
}
```

Run: `flutter test test/data/event_vorjahr_test.dart` → erst FAIL, nach Implementierung PASS.

- [ ] **Step 5: Provider**

`event_providers.dart`:

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:sbs_projer_app/data/local/event_local_export.dart';
import 'package:sbs_projer_app/data/local/event_kontakt_local_export.dart';
import 'package:sbs_projer_app/data/repositories/event_repository.dart';
import 'package:sbs_projer_app/data/repositories/event_kontakt_repository.dart';

/// Alle Event-Jahre des Users.
final eventsProvider = FutureProvider<List<EventLocal>>((ref) async {
  return EventRepository.getAll();
});

/// Ein Event-Jahr per routeId.
final eventByIdProvider =
    FutureProvider.family<EventLocal?, String>((ref, id) async {
  return EventRepository.getById(id);
});

/// Kontakt-Zuordnungen eines Event-Jahres.
final eventKontakteProvider =
    FutureProvider.family<List<EventKontaktLocal>, String>((ref, eventId) async {
  return EventKontaktRepository.getByEvent(eventId);
});
```

- [ ] **Step 6: Analyze + Tests + Commit**

Run: `flutter analyze && flutter test` — keine neuen Findings, alle Tests grün.

```bash
git add sbs_projer_app/lib/data/mappers/event_mapper.dart sbs_projer_app/lib/data/mappers/event_kontakt_mapper.dart sbs_projer_app/lib/data/repositories/event_repository.dart sbs_projer_app/lib/data/repositories/event_kontakt_repository.dart sbs_projer_app/lib/presentation/providers/event_providers.dart sbs_projer_app/test/data/event_vorjahr_test.dart
git commit -m "feat(events): Mapper, Repositories, Provider, Vorjahres-Uebernahme inkl. Test (E1)"
```

---

### Task 6: Sync-Integration

**Files:**
- Modify: `sbs_projer_app/lib/services/sync/sync_service.dart` (Tier-Listen Z. 107–151 + zwei neue Methoden nach `_syncBetriebKontakte` Z. 393–419)

- [ ] **Step 1: `_syncEvents` + `_syncEventKontakte` schreiben**

Exakt nach dem Muster `_syncBetriebKontakte` (Z. 393–419), mit `EventLocal`/`EventMapper`/Tabelle `'events'` bzw. `EventKontaktLocal`/`EventKontaktMapper`/Tabelle `'event_kontakte'` und Meta-Keys `'events'` / `'event_kontakte'`. Wichtig: identisches Last-Write-Wins-Muster (`ex.isSynced == false && ex.lastModifiedAt.isAfter(...) → skip`).

- [ ] **Step 2: In Tier-Struktur einhängen**

- `_syncEvents` in **Tier 2** (nach Betriebe, neben `_syncBetriebKontakte`).
- `_syncEventKontakte` in **Tier 3** (braucht events + kontakte).
Vorher die Tier-Listen bei Z. 107–151 lesen und die Aufrufe an der FK-korrekten Stelle ergänzen.

- [ ] **Step 3: Analyze + Commit**

Run: `flutter analyze` — keine neuen Findings.

```bash
git add sbs_projer_app/lib/services/sync/sync_service.dart
git commit -m "feat(events): Sync-Integration events + event_kontakte (E1)"
```

---

### Task 7: Events-Liste, Routen, Dashboard-Kachel

**Files:**
- Create: `sbs_projer_app/lib/presentation/screens/events/events_list_screen.dart`
- Modify: `sbs_projer_app/lib/core/config/router.dart`
- Modify: `sbs_projer_app/lib/presentation/screens/home_screen.dart`

- [ ] **Step 1: Events-Liste bauen**

`events_list_screen.dart` — `ConsumerWidget`, Stil-Referenz `kontakte_list_screen.dart`:
- `ref.watch(eventsProvider)` + `ref.watch(betriebNameMapProvider)` + `betriebOrtMapProvider`.
- Titel pro Karte: `'${betriebName} ${event.jahr}'`; Untertitel: Termin (`dd.MM.–dd.MM.yyyy`, oder «Termin offen») + Ort.
- Status-Badge über `eventStatus(e.terminVon, e.terminBis, DateTime.now())`:
  laufend = grün «laufend», kommend = blau «in X Tagen» (X = Differenz zu terminVon),
  offen = grau «Termin offen», vorbei = grau «vorbei».
- Sortierung: laufend zuerst, dann kommend (terminVon aufsteigend), dann offen (jahr absteigend), dann vorbei (jahr absteigend, dann terminVon absteigend):

```dart
int _sortKey(EventStatus s) => switch (s) {
      EventStatus.laufend => 0,
      EventStatus.kommend => 1,
      EventStatus.offen => 2,
      EventStatus.vorbei => 3,
    };
```

- SearchBar (Filter auf Betriebsname/Ort/Jahr, Muster wie Störungen-Liste).
- Tap → `context.push('/events/${e.routeId}')`; FAB (+) → `context.push('/events/neu')`.
- Nach Rückkehr von Detail/Form: `ref.invalidate(eventsProvider)` (Pattern: `await context.push(...)` dann invalidate).

- [ ] **Step 2: Routen ergänzen**

In `router.dart` nach dem Kontakte-Block (Z. 548 ff.), Muster exakt wie dort:

```dart
      GoRoute(
        path: '/events',
        builder: (context, state) => const EventsListScreen(),
      ),
      GoRoute(
        path: '/events/neu',
        builder: (context, state) => const EventFormScreen(),
      ),
      GoRoute(
        path: '/events/:id',
        builder: (context, state) =>
            EventDetailScreen(eventId: state.pathParameters['id']!),
      ),
      GoRoute(
        path: '/events/:id/bearbeiten',
        builder: (context, state) =>
            EventFormScreen(eventId: state.pathParameters['id']),
      ),
```

(EventFormScreen/EventDetailScreen entstehen in Task 8/9 — Task 7 legt dafür minimale Platzhalter-Screens an ODER Task 7–9 werden gemeinsam committet; Entscheidung: Routen + Kachel erst in diesem Task einbauen, aber `EventFormScreen`/`EventDetailScreen` hier bereits als einfache Scaffold-Stubs mit AppBar anlegen, damit analyze grün ist. Die Stubs werden in Task 8/9 ersetzt.)

- [ ] **Step 3: Dashboard-Kachel**

In `home_screen.dart` im Kachel-Grid (Muster Z. 81–129, `_DashboardTile`):

```dart
              _DashboardTile(
                icon: Icons.festival,
                label: 'Events',
                count: eventCount > 0 ? '$eventCount' : null,
                color: Colors.deepPurple,
                onTap: () => context.push('/events'),
              ),
```

`eventCount` = Anzahl Event-Jahre im aktuellen Kalenderjahr, via `ref.watch(eventsProvider)` (`valueOrNull`-Muster wie die anderen Counts im Screen; vorher ansehen, wie kontaktCount dort berechnet wird, und identisch machen).

- [ ] **Step 4: Analyze + Commit**

```bash
git add sbs_projer_app/lib/presentation/screens/events/ sbs_projer_app/lib/core/config/router.dart sbs_projer_app/lib/presentation/screens/home_screen.dart
git commit -m "feat(events): Events-Liste, Routen, Dashboard-Kachel (E1)"
```

---

### Task 8: Event-Formular (anlegen/bearbeiten, Vorjahres-Checkbox)

**Files:**
- Create/Replace Stub: `sbs_projer_app/lib/presentation/screens/events/event_form_screen.dart`

- [ ] **Step 1: Formular bauen**

`EventFormScreen({this.eventId})` — `ConsumerStatefulWidget`, Stil-Referenz `kontakt_form_screen.dart`:
- **Betrieb-Auswahl** (nur bei Neu-Anlage änderbar): Dropdown/Auswahl-Sheet über `ref.watch(betriebeProvider)` gefiltert auf `b.zapfsysteme.contains('Veranstaltungen')` (exakte Schreibweise, grosses V!). Ist die Liste leer: Hinweis-Card «Kein Veranstaltungs-Betrieb vorhanden — zuerst Betrieb mit Zapfsystem ‹Veranstaltungen› anlegen».
- **Jahr**: `TextFormField` numerisch, Default `DateTime.now().year`, Validator 2020–2100.
- **Termin von/bis**: zwei DatePicker-Felder in einer Row (Muster `_DatePickerField` aus `betrieb_form_screen.dart` — lokale Kopie als privates Widget, falls nicht importierbar).
- **Notizen**: mehrzeiliges TextFormField.
- **Checkbox «Kontakte aus Vorjahr übernehmen»**: nur bei Neu-Anlage; sichtbar sobald Betrieb gewählt UND `EventRepository.getVorjahr(betriebId, jahr)` ein Event liefert (bei Betrieb-/Jahr-Änderung neu prüfen); Label zeigt das Quellenjahr: «Kontakte aus ‹Name Jahr› übernehmen»; Default: angehakt.
- **Speichern**:

```dart
    final e = _existing ?? EventLocal();
    e.betriebId = _betriebId!;
    e.jahr = _jahr;
    e.terminVon = _terminVon;
    e.terminBis = _terminBis;
    e.notizen = _notizenController.text.trim().isEmpty
        ? null
        : _notizenController.text.trim();
    await EventRepository.save(e);
    if (_uebernehmeVorjahr && _vorjahrEvent != null) {
      final n = await EventKontaktRepository.uebernehmeVon(
          _vorjahrEvent!.serverId!, e.serverId!);
      // Snackbar: «$n Kontakte übernommen»
    }
```

- Duplikat-Schutz: vor dem Speichern prüfen, ob (betriebId, jahr) schon existiert (ausser beim Bearbeiten desselben Events) → Fehlermeldung statt DB-Constraint-Crash.
- Danach `ref.invalidate(eventsProvider)` + bei Neuanlage `context.pushReplacement('/events/${e.routeId}')`, bei Bearbeiten `pop`.
- Bearbeiten-Modus: `eventId != null` → Event via `EventRepository.getById` laden, Felder füllen, Betrieb read-only anzeigen.

- [ ] **Step 2: Analyze + Commit**

```bash
git add sbs_projer_app/lib/presentation/screens/events/event_form_screen.dart
git commit -m "feat(events): Event-Formular mit Vorjahres-Uebernahme (E1)"
```

---

### Task 9: Event-Detail — Kontaktliste, Zuordnung, WhatsApp/Anruf

**Files:**
- Create/Replace Stub: `sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart`

- [ ] **Step 1: Detail-Screen bauen**

`EventDetailScreen({required this.eventId})` — `ConsumerStatefulWidget`:

**Kopf** (Card): Name (Betrieb + Jahr), Termin, Status-Badge (wie Liste), Ort des Betriebs, Notizen. AppBar-Actions: Bearbeiten (→ `/events/$eventId/bearbeiten`), PopupMenu mit «Aus Vorjahr übernehmen» und «Event löschen» (Lösch-Dialog mit Bestätigung; Reihenfolge: zuerst `EventKontaktRepository.deleteByEvent(event.serverId!)`, dann `EventRepository.delete(event.routeId)`, danach pop + invalidate).

**Kontaktliste**: `ref.watch(eventKontakteProvider(eventServerId))` + `ref.watch(kontakteProvider)` (Map kontaktId → KontaktLocal bauen). Gruppiert nach `EventKontakt.rollenReihenfolge`; pro Gruppe Abschnittstitel `EventKontakt.rolleLabel(rolle)` + Zählerchip. Pro Zeile:

```dart
        ListTile(
          title: Text('${k.vorname} ${k.nachname ?? ''}'.trim()),
          subtitle: (z.bemerkung != null && z.bemerkung!.isNotEmpty)
              ? Text(z.bemerkung!)
              : (k.telefon != null ? Text(k.telefon!) : null),
          trailing: Row(
            mainAxisSize: MainAxisSize.min,
            children: [
              if (whatsappNummer(k.telefon) != null)
                IconButton(
                  tooltip: 'WhatsApp',
                  icon: const Icon(Icons.chat, color: Color(0xFF25D366)),
                  onPressed: () => launchUrl(whatsappUri(k.telefon!),
                      mode: LaunchMode.externalApplication),
                ),
              if (k.telefon != null)
                IconButton(
                  tooltip: 'Anrufen',
                  icon: const Icon(Icons.phone),
                  onPressed: () =>
                      launchUrl(Uri.parse('tel:${k.telefon!.replaceAll(' ', '')}')),
                ),
            ],
          ),
        )
```

Zeile lang drücken oder Swipe (Dismissible, Muster Tagesplan) → Zuordnung entfernen (`EventKontaktRepository.delete(z.routeId)` + invalidate; Hinweistext «Nur die Zuordnung wird entfernt, nicht der Kontakt»).

**«Aus Vorjahr übernehmen»** (PopupMenu): `getVorjahr` → falls vorhanden `uebernehmeVon(...)` → Snackbar «X Kontakte übernommen» (auch «0 Kontakte übernommen» ist ok) + invalidate. Falls kein Vorjahr: Snackbar «Kein Vorjahres-Event vorhanden».

**FAB «Kontakt zuordnen»** → `showModalBottomSheet` (DraggableScrollableSheet, Muster Tourenplanungs-Filter):
1. Suchfeld + Liste ALLER Kontakte (`kontakteProvider`, Filter Name/Telefon/Kategorie-Label; bereits vollständig zugeordnete Kontakte werden weiter angezeigt — Mehrfachrollen sind erlaubt).
2. Kopfzeile-Button «Neuer Kontakt» → `await context.push('/kontakte/neu?kategorie=event')`, danach `ref.invalidate(kontakteProvider)` und Sheet-Liste aktualisieren.
3. Tap auf Kontakt → zweiter Schritt im Sheet: Rolle-Dropdown (`EventKontakt.rollenReihenfolge` mit `rolleLabel`, Default `'stand'`), Bemerkung-TextField, Button «Zuordnen» →

```dart
      final z = EventKontaktLocal()
        ..eventId = eventServerId
        ..kontaktId = kontakt.serverId!
        ..rolle = _rolle
        ..bemerkung = _bemerkung.trim().isEmpty ? null : _bemerkung.trim();
      await EventKontaktRepository.save(z);
```

   Duplikat (kontaktId, rolle) vorher prüfen → Snackbar «Bereits zugeordnet» statt Constraint-Fehler. Danach invalidate + Sheet schliessen.

- [ ] **Step 2: Analyze + alle Tests**

Run: `flutter analyze && flutter test` — keine neuen Findings, alles grün.

- [ ] **Step 3: Commit**

```bash
git add sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): Event-Detail mit Kontaktliste, Zuordnung, WhatsApp/Anruf (E1)"
```

---

### Task 10: Gesamtverifikation, visueller Test, Deploy, Doku

- [ ] **Step 1: Voller Lauf**

Run: `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && flutter analyze && flutter test`
Expected: 0 neue errors/warnings (47 Altbestand-infos), alle Tests grün (210 + neue).

- [ ] **Step 2: Visueller Browser-Test (PFLICHT vor Deploy)**

Lokaler Build (`flutter build web --pwa-strategy=none`, ohne base-href) + Server (launch.json `flutter-web`), Checkliste:
1. Dashboard: Kachel «Events» mit Zähler
2. Events-Liste: leer-Zustand, dann Event anlegen (Veranstaltungs-Betrieb wählbar, Jahr, Termin)
3. Zweites Jahr anlegen → Vorjahres-Checkbox erscheint, Übernahme funktioniert («X Kontakte übernommen»)
4. Detail: Kontakt zuordnen (bestehender + neuer Kontakt), Gruppierung nach Rollen, Bemerkung sichtbar
5. WhatsApp-Button: öffnet `wa.me/41…` (Linkziel prüfen), Anruf-Button `tel:`
6. Zuordnung entfernen; Event bearbeiten (Termin ändern → Status-Badge ändert sich); Event löschen
7. Duplikat-Schutz: gleiches Jahr + Betrieb nochmals anlegen → saubere Fehlermeldung

- [ ] **Step 3: Version bumpen**

`pubspec.yaml`: `0.16.20+478` → `0.17.0+479` (neues Feature-Modul).

- [ ] **Step 4: Deploy gemäss CLAUDE.md**

Alle Änderungen committen + auf main mergen + pushen, dann Build mit `--base-href "/sbs-projer-dev/"` + `--pwa-strategy=none`, Cache-Bust, gh-pages-Deploy, zurück auf main.

- [ ] **Step 5: Doku nachführen**

`ToDo.md`: E1 als erledigt eintragen (mit Datum/Version), E2–E4 als nächste Phasen; `Projekt.md`-Header (Stand, Version, «Zuletzt»-Absatz). Commit + Push.
