# Events E4 — Zeit-/Spesenerfassung + Auto-Montage — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Am Event Zeit (Anfahrt/Inbetriebnahme/Pikett) und Spesen pro Tag erfassen und daraus die Abrechnungs-Montage (Typ „Anlass") vorbefüllt erzeugen.

**Architecture:** Neue Sync-Vertikale `event_aufwand` (DTO/Isar-Local/Web-Stub/Mapper/Repo/Provider/Sync nach E1–E3-Muster). Neuer 5. Tab „Zeit" im Event-Detail mit Erfassungs-Formular + Total-Stunden. Reine Aggregations-Funktion (Aufwand→Montage-Slots pro Tag, ≤5 Slots) mit Unit-Tests. Bestehendes `MontageFormScreen` bekommt einen additiven, optionalen `vorbefuellung`-Parameter.

**Tech Stack:** Flutter, Isar (offline-first native) + Supabase (web), Riverpod, GoRouter. Spec: `docs/superpowers/specs/2026-07-10-events-e4-design.md`.

---

## Referenz-Muster (bestehende E3-Vertikale `event_einsatz`)

Diese Dateien sind die 1:1-Vorlage — beim Bauen anschauen:
- DTO: `sbs_projer_app/lib/data/models/event_einsatz.dart`
- Local: `sbs_projer_app/lib/data/local/event_einsatz_local.dart`
- Web-Stub: `sbs_projer_app/lib/data/local/web/event_einsatz_local_web.dart`
- Export: `sbs_projer_app/lib/data/local/event_einsatz_local_export.dart`
- Mapper: `sbs_projer_app/lib/data/mappers/event_einsatz_mapper.dart`
- Repo: `sbs_projer_app/lib/data/repositories/event_einsatz_repository.dart`
- Provider: `sbs_projer_app/lib/presentation/providers/event_providers.dart:39`
- IsarService: `sbs_projer_app/lib/services/storage/isar_service.dart:298` + Web-Stub `isar_service_web.dart:164`
- Sync: `sbs_projer_app/lib/services/sync/sync_service.dart:570`

**Wichtige Gotchas (CLAUDE.md):**
- Alle Imports von Local-Klassen über `*_export.dart`, nie direkt (außer in `isar_service.dart` + `sync_service.dart`, die `event_*_local.dart` direkt importieren müssen, weil dort Isar-Extensions genutzt werden).
- `.g.dart`-Dateien sind gitignored — **niemals stagen**.
- **Kein** `?ausdruck` (null-aware Collection-Elemente) in `@collection`-Klassen — bricht isar_generator. Stattdessen `if (x != null) …`.
- build_runner-Befehl: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && dart run build_runner build --delete-conflicting-outputs`
- `flutter analyze` und Tests: PATH wie oben setzen.

---

## Task 1: Migration 123 — Tabelle `event_aufwand`

**Files:**
- Create: `Datenbank/migrations/123_events_e4.sql`

- [ ] **Step 1: SQL-Datei schreiben**

```sql
-- ============================================================
-- Migration 123: Events E4 — Zeit-/Spesenerfassung (event_aufwand)
-- Projekt: SBS Projer App
-- Stand: 10.07.2026
-- ============================================================

CREATE TABLE event_aufwand (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  event_id uuid NOT NULL REFERENCES events(id) ON DELETE CASCADE,
  datum date NOT NULL,
  kategorie text NOT NULL CHECK (kategorie IN ('anfahrt','inbetriebnahme','pikett','spesen')),
  notiz text,
  stunden numeric NOT NULL DEFAULT 0,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
ALTER TABLE event_aufwand ENABLE ROW LEVEL SECURITY;
CREATE POLICY event_aufwand_user_policy ON event_aufwand
  FOR ALL USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
CREATE TRIGGER update_event_aufwand_updated_at BEFORE UPDATE ON event_aufwand
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE INDEX idx_event_aufwand_event ON event_aufwand(user_id, event_id);
```

- [ ] **Step 2: Migration auf Prod anwenden**

Über den Supabase-MCP-Server `mcp__supabase__apply_migration` (Project-ID `pltbaqqwpnmdajwgnhpd`, name `events_e4_event_aufwand`) mit dem SQL aus Step 1 (ohne die Kommentar-Kopfzeilen ist ok).

- [ ] **Step 3: Anwendung verifizieren**

Via `mcp__supabase__execute_sql`:
```sql
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'event_aufwand' ORDER BY ordinal_position;
```
Erwartet: id/user_id/event_id (uuid), datum (date), kategorie/notiz (text), stunden (numeric), created_at/updated_at (timestamp with time zone).

- [ ] **Step 4: Commit**

```bash
git add Datenbank/migrations/123_events_e4.sql
git commit -m "feat(events): Migration 123 event_aufwand (E4 Zeit-/Spesenerfassung)"
```

---

## Task 2: DTO `EventAufwand`

**Files:**
- Create: `sbs_projer_app/lib/data/models/event_aufwand.dart`
- Test: `sbs_projer_app/test/data/models/event_aufwand_test.dart`

- [ ] **Step 1: Failing Test schreiben**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:sbs_projer_app/data/models/event_aufwand.dart';

void main() {
  test('fromJson/toJson roundtrip', () {
    final json = {
      'id': 'a1',
      'user_id': 'u1',
      'event_id': 'e1',
      'datum': '2026-07-25',
      'kategorie': 'pikett',
      'notiz': '10:00-02:00',
      'stunden': 16,
    };
    final dto = EventAufwand.fromJson(json);
    expect(dto.eventId, 'e1');
    expect(dto.datum, DateTime(2026, 7, 25));
    expect(dto.kategorie, 'pikett');
    expect(dto.stunden, 16.0);

    final out = dto.toJson();
    expect(out['event_id'], 'e1');
    expect(out['datum'], '2026-07-25');
    expect(out['kategorie'], 'pikett');
    expect(out['stunden'], 16.0);
    expect(out['id'], 'a1');
  });
}
```

- [ ] **Step 2: Test ausführen — muss fehlschlagen**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter test test/data/models/event_aufwand_test.dart`
Expected: FAIL („Target of URI doesn't exist" / EventAufwand not defined).

- [ ] **Step 3: DTO implementieren**

`stunden` als `double` (numeric kann int oder double liefern → über Helfer parsen). `datum` als Date-only DateTime.

```dart
/// Supabase-DTO für eine Zeit-/Spesen-Zeile eines Events (E4).
class EventAufwand {
  final String id;
  final String userId;
  final String eventId;
  final DateTime datum;
  final String kategorie; // anfahrt | inbetriebnahme | pikett | spesen
  final String? notiz;
  final double stunden;
  final DateTime? createdAt;
  final DateTime? updatedAt;

  EventAufwand({
    required this.id,
    required this.userId,
    required this.eventId,
    required this.datum,
    required this.kategorie,
    this.notiz,
    required this.stunden,
    this.createdAt,
    this.updatedAt,
  });

  static double _toDouble(dynamic v) =>
      v == null ? 0.0 : (v is num ? v.toDouble() : double.tryParse(v.toString()) ?? 0.0);

  factory EventAufwand.fromJson(Map<String, dynamic> json) {
    return EventAufwand(
      id: json['id'],
      userId: json['user_id'],
      eventId: json['event_id'],
      datum: DateTime.parse(json['datum']),
      kategorie: json['kategorie'],
      notiz: json['notiz'],
      stunden: _toDouble(json['stunden']),
      createdAt: json['created_at'] != null ? DateTime.parse(json['created_at']) : null,
      updatedAt: json['updated_at'] != null ? DateTime.parse(json['updated_at']) : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'user_id': userId,
      'event_id': eventId,
      'datum': datum.toIso8601String().substring(0, 10),
      'kategorie': kategorie,
      'notiz': notiz,
      'stunden': stunden,
    };
  }
}
```

- [ ] **Step 4: Test ausführen — muss bestehen**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter test test/data/models/event_aufwand_test.dart`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add sbs_projer_app/lib/data/models/event_aufwand.dart sbs_projer_app/test/data/models/event_aufwand_test.dart
git commit -m "feat(events): EventAufwand DTO (E4)"
```

---

## Task 3: Isar-Local + Web-Stub + Export + IsarService + Schema + build_runner

**Files:**
- Create: `sbs_projer_app/lib/data/local/event_aufwand_local.dart`
- Create: `sbs_projer_app/lib/data/local/web/event_aufwand_local_web.dart`
- Create: `sbs_projer_app/lib/data/local/event_aufwand_local_export.dart`
- Modify: `sbs_projer_app/lib/services/storage/isar_service.dart` (Import, Schema-Registrierung, Query-Methoden)
- Modify: `sbs_projer_app/lib/services/storage/isar_service_web.dart` (Stub-Methoden)

- [ ] **Step 1: Isar-Local schreiben**

`sbs_projer_app/lib/data/local/event_aufwand_local.dart`:
```dart
import 'package:flutter/foundation.dart' show kIsWeb;
import 'package:isar/isar.dart';

part 'event_aufwand_local.g.dart';

@collection
class EventAufwandLocal {
  Id id = Isar.autoIncrement;

  @ignore
  String get routeId => kIsWeb ? serverId! : id.toString();

  // Supabase Sync
  @Index()
  String? serverId;
  @Index()
  bool isSynced = false;
  DateTime? lastModifiedAt;

  // Felder
  late String userId;
  @Index()
  late String eventId;
  late DateTime datum;
  late String kategorie;
  String? notiz;
  late double stunden;
  DateTime? createdAt;
  DateTime? updatedAt;
}
```

- [ ] **Step 2: Web-Stub schreiben**

`sbs_projer_app/lib/data/local/web/event_aufwand_local_web.dart`:
```dart
/// Web-Stub für EventAufwandLocal (kein Isar auf Web).
class EventAufwandLocal {
  int id = 0;

  String get routeId => serverId!;

  // Supabase Sync
  String? serverId;
  bool isSynced = false;
  DateTime? lastModifiedAt;

  // Felder
  String userId = '';
  String eventId = '';
  DateTime datum = DateTime(2000);
  String kategorie = 'pikett';
  String? notiz;
  double stunden = 0;
  DateTime? createdAt;
  DateTime? updatedAt;
}
```

- [ ] **Step 3: Conditional Export schreiben**

`sbs_projer_app/lib/data/local/event_aufwand_local_export.dart`:
```dart
export 'event_aufwand_local.dart' if (dart.library.html) 'web/event_aufwand_local_web.dart';
```

- [ ] **Step 4: IsarService erweitern**

In `sbs_projer_app/lib/services/storage/isar_service.dart`:

(a) Import neben den anderen event-Imports (z. B. nach `import 'package:sbs_projer_app/data/local/event_einsatz_local.dart';`):
```dart
import 'package:sbs_projer_app/data/local/event_aufwand_local.dart';
```

(b) Schema-Registrierung — in der Schema-Liste (nach `EventDokumentLocalSchema,`, aktuell um Zeile 61) ergänzen:
```dart
        EventAufwandLocalSchema,
```

(c) Query-Methoden — direkt nach dem `// ─── EventEinsatz ───`-Block (nach Zeile 308) einfügen:
```dart
  // ─── EventAufwand ───
  static Future<List<EventAufwandLocal>> eventAufwandFindByEvent(String eventId) =>
      instance.eventAufwandLocals.filter().eventIdEqualTo(eventId).findAll();
  static Future<EventAufwandLocal?> eventAufwandGet(int id) =>
      instance.eventAufwandLocals.get(id);
  static Future<EventAufwandLocal?> eventAufwandFindByServerId(String serverId) =>
      instance.eventAufwandLocals.filter().serverIdEqualTo(serverId).findFirst();
  static Future<void> eventAufwandPut(EventAufwandLocal e) =>
      instance.writeTxn(() => instance.eventAufwandLocals.put(e));
  static Future<void> eventAufwandDelete(int id) =>
      instance.writeTxn(() => instance.eventAufwandLocals.delete(id));
```

- [ ] **Step 5: IsarService-Web-Stub erweitern**

In `sbs_projer_app/lib/services/storage/isar_service_web.dart` nach dem `// ─── EventEinsatz ───`-Block (nach Zeile 169) einfügen:
```dart
  // ─── EventAufwand ───
  static dynamic eventAufwandFindByEvent(String eventId) => throw UnsupportedError('Isar not available on web');
  static dynamic eventAufwandGet(int id) => throw UnsupportedError('Isar not available on web');
  static dynamic eventAufwandFindByServerId(String serverId) => throw UnsupportedError('Isar not available on web');
  static dynamic eventAufwandPut(dynamic e) => throw UnsupportedError('Isar not available on web');
  static dynamic eventAufwandDelete(int id) => throw UnsupportedError('Isar not available on web');
```

- [ ] **Step 6: build_runner ausführen**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && dart run build_runner build --delete-conflicting-outputs`
Expected: „Succeeded" — erzeugt `event_aufwand_local.g.dart`. Bei „Failed" prüfen, ob irgendwo `?ausdruck`-Syntax steht.

- [ ] **Step 7: analyze**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter analyze`
Expected: keine neuen Fehler (bestehende Infos ok).

- [ ] **Step 8: Commit** (`.g.dart` NICHT stagen)

```bash
git add sbs_projer_app/lib/data/local/event_aufwand_local.dart \
        sbs_projer_app/lib/data/local/web/event_aufwand_local_web.dart \
        sbs_projer_app/lib/data/local/event_aufwand_local_export.dart \
        sbs_projer_app/lib/services/storage/isar_service.dart \
        sbs_projer_app/lib/services/storage/isar_service_web.dart
git commit -m "feat(events): EventAufwand Isar-Local + IsarService (E4)"
```

---

## Task 4: Mapper `EventAufwandMapper`

**Files:**
- Create: `sbs_projer_app/lib/data/mappers/event_aufwand_mapper.dart`

- [ ] **Step 1: Mapper schreiben**

```dart
import 'package:sbs_projer_app/data/local/event_aufwand_local_export.dart';
import 'package:sbs_projer_app/data/models/event_aufwand.dart';

class EventAufwandMapper {
  static EventAufwandLocal fromDto(EventAufwand dto, {EventAufwandLocal? existing}) {
    final local = existing ?? EventAufwandLocal();
    local.serverId = dto.id;
    local.userId = dto.userId;
    local.eventId = dto.eventId;
    local.datum = dto.datum;
    local.kategorie = dto.kategorie;
    local.notiz = dto.notiz;
    local.stunden = dto.stunden;
    local.createdAt = dto.createdAt;
    local.updatedAt = dto.updatedAt;
    local.isSynced = true;
    local.lastModifiedAt = dto.updatedAt ?? dto.createdAt ?? DateTime.now();
    return local;
  }

  static Map<String, dynamic> toJson(EventAufwandLocal local) {
    final json = <String, dynamic>{
      'user_id': local.userId,
      'event_id': local.eventId,
      'datum': local.datum.toIso8601String().substring(0, 10),
      'kategorie': local.kategorie,
      'notiz': local.notiz,
      'stunden': local.stunden,
    };
    if (local.serverId != null) json['id'] = local.serverId;
    return json;
  }
}
```

- [ ] **Step 2: analyze**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter analyze lib/data/mappers/event_aufwand_mapper.dart`
Expected: keine Fehler.

- [ ] **Step 3: Commit**

```bash
git add sbs_projer_app/lib/data/mappers/event_aufwand_mapper.dart
git commit -m "feat(events): EventAufwandMapper (E4)"
```

---

## Task 5: Repository + Provider

**Files:**
- Create: `sbs_projer_app/lib/data/repositories/event_aufwand_repository.dart`
- Modify: `sbs_projer_app/lib/presentation/providers/event_providers.dart`

- [ ] **Step 1: Repository schreiben**

`getByEvent` sortiert nach Datum aufsteigend (Server-seitig `order('datum')`; native über Provider, siehe Step 2).
```dart
import 'package:flutter/foundation.dart' show kIsWeb;
import 'package:uuid/uuid.dart';
import 'package:sbs_projer_app/data/local/event_aufwand_local_export.dart';
import 'package:sbs_projer_app/data/models/event_aufwand.dart';
import 'package:sbs_projer_app/data/mappers/event_aufwand_mapper.dart';
import 'package:sbs_projer_app/services/storage/isar_service_export.dart';
import 'package:sbs_projer_app/services/supabase/supabase_service.dart';

class EventAufwandRepository {
  static String get _userId => SupabaseService.dataUserId;

  static Future<List<EventAufwandLocal>> getByEvent(String eventId) async {
    if (kIsWeb) {
      final rows = await SupabaseService.client
          .from('event_aufwand').select()
          .eq('user_id', _userId)
          .eq('event_id', eventId)
          .order('datum', ascending: true);
      return rows
          .map((r) => EventAufwandMapper.fromDto(EventAufwand.fromJson(r)))
          .toList();
    }
    return IsarService.eventAufwandFindByEvent(eventId);
  }

  static Future<void> save(EventAufwandLocal aufwand) async {
    aufwand.userId = _userId;
    aufwand.serverId ??= const Uuid().v4();
    if (kIsWeb) {
      final json = EventAufwandMapper.toJson(aufwand);
      await SupabaseService.client.from('event_aufwand').upsert(json);
      return;
    }
    aufwand.isSynced = false;
    aufwand.lastModifiedAt = DateTime.now().toUtc();
    await IsarService.eventAufwandPut(aufwand);
  }

  static Future<void> delete(String id) async {
    if (kIsWeb) {
      await SupabaseService.client.from('event_aufwand').delete().eq('id', id);
      return;
    }
    // Native: zuerst serverseitig löschen, dann Isar aufräumen.
    final isarId = int.parse(id);
    final local = await IsarService.eventAufwandGet(isarId);
    if (local?.serverId != null) {
      await SupabaseService.client
          .from('event_aufwand').delete().eq('id', local!.serverId!);
    }
    await IsarService.eventAufwandDelete(isarId);
  }
}
```

- [ ] **Step 2: Provider ergänzen**

In `sbs_projer_app/lib/presentation/providers/event_providers.dart`:

(a) Imports (bei den anderen event-Imports):
```dart
import 'package:sbs_projer_app/data/local/event_aufwand_local_export.dart';
import 'package:sbs_projer_app/data/repositories/event_aufwand_repository.dart';
```

(b) Provider ans Ende der Datei anfügen (sortiert nach Datum aufsteigend — deckt auch Native ab):
```dart
/// Zeit-/Spesen-Zeilen eines Event-Jahres (nach Datum aufsteigend).
final eventAufwaendeProvider =
    FutureProvider.family<List<EventAufwandLocal>, String>((ref, eventId) async {
  final list = await EventAufwandRepository.getByEvent(eventId);
  list.sort((a, b) => a.datum.compareTo(b.datum));
  return list;
});
```

- [ ] **Step 3: analyze**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter analyze lib/data/repositories/event_aufwand_repository.dart lib/presentation/providers/event_providers.dart`
Expected: keine Fehler.

- [ ] **Step 4: Commit**

```bash
git add sbs_projer_app/lib/data/repositories/event_aufwand_repository.dart \
        sbs_projer_app/lib/presentation/providers/event_providers.dart
git commit -m "feat(events): EventAufwand Repository + Provider (E4)"
```

---

## Task 6: Sync-Integration (`_syncEventAufwand`, Tier 3)

**Files:**
- Modify: `sbs_projer_app/lib/services/sync/sync_service.dart`

`event_aufwand` hängt nur am Event (kein Stand-FK) → **Tier 3** (wie `event_dokumente`/`event_staende`).

- [ ] **Step 1: Imports ergänzen**

Bei den anderen event-Imports von sync_service.dart (direkt-Import der Local-Klasse, DTO, Mapper):
```dart
import 'package:sbs_projer_app/data/local/event_aufwand_local.dart';
import 'package:sbs_projer_app/data/models/event_aufwand.dart';
import 'package:sbs_projer_app/data/mappers/event_aufwand_mapper.dart';
```

- [ ] **Step 2: Sync-Aufruf in Tier 3 registrieren**

In der Tier-3-Liste (aktuell Zeilen 158–166, nach `() => _syncEventStaende(userId),`) ergänzen:
```dart
          () => _syncEventAufwand(userId),
```

- [ ] **Step 3: Sync-Methode implementieren**

Nach `_syncEventDokumente` (um Zeile 536) oder neben den anderen Event-Sync-Methoden einfügen:
```dart
  // Event-Aufwand / Zeit (Tier 3: → Event)
  static Future<({int pushed, int pulled})> _syncEventAufwand(String uid) async {
    final unsynced =
        await _isar.eventAufwandLocals.filter().isSyncedEqualTo(false).findAll();
    final pushed = await _pushToSupabase<EventAufwandLocal>(
      'event_aufwand', unsynced, EventAufwandMapper.toJson,
      (l, id) { l.serverId ??= id; l.isSynced = true; },
    );
    if (pushed.isNotEmpty) {
      await _isar.writeTxn(() => _isar.eventAufwandLocals.putAll(pushed));
    }

    final rows = await _pullRows('event_aufwand', 'event_aufwand', uid);
    final toSave = <EventAufwandLocal>[];
    for (final row in rows) {
      final dto = EventAufwand.fromJson(row);
      final ex = await _isar.eventAufwandLocals.filter().serverIdEqualTo(dto.id).findFirst();
      if (ex != null && !ex.isSynced &&
          (ex.lastModifiedAt?.isAfter(dto.updatedAt ?? DateTime(2000)) ?? false)) {
        continue;
      }
      toSave.add(EventAufwandMapper.fromDto(dto, existing: ex));
    }
    if (toSave.isNotEmpty) {
      await _isar.writeTxn(() => _isar.eventAufwandLocals.putAll(toSave));
    }
    await _updateMeta('event_aufwand');
    return (pushed: pushed.length, pulled: toSave.length);
  }
```

- [ ] **Step 4: analyze**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter analyze lib/services/sync/sync_service.dart`
Expected: keine Fehler.

- [ ] **Step 5: Commit**

```bash
git add sbs_projer_app/lib/services/sync/sync_service.dart
git commit -m "feat(events): Sync event_aufwand (Tier 3, E4)"
```

---

## Task 7: Aggregations-Util `montageSlotsAusAufwand` (TDD)

**Files:**
- Create: `sbs_projer_app/lib/core/util/event_aufwand_slots.dart`
- Test: `sbs_projer_app/test/core/util/event_aufwand_slots_test.dart`

Aggregiert Aufwand-Zeilen **pro Tag** zu Montage-Anlass-Slots. Da „Anlass" nur die ersten 5 Slots summiert, bei > 5 Tagen: erste 4 Tage einzeln + 5. Sammel-Slot „Weitere Tage" (Rest-Summe). Gesamt-Stunden bleiben erhalten.

- [ ] **Step 1: Failing Test schreiben**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:sbs_projer_app/core/util/event_aufwand_slots.dart';

void main() {
  group('montageSlotsAusAufwand', () {
    test('leere Liste -> keine Slots', () {
      expect(montageSlotsAusAufwand([]), isEmpty);
    });

    test('ein Tag -> ein Slot mit Summe und Datums-Label', () {
      final slots = montageSlotsAusAufwand([
        (datum: DateTime(2026, 7, 25), stunden: 4),
        (datum: DateTime(2026, 7, 25), stunden: 12),
      ]);
      expect(slots.length, 1);
      expect(slots.first.stunden, 16);
      expect(slots.first.text, 'Fr 25.7.');
    });

    test('mehrere Tage nach Datum sortiert', () {
      final slots = montageSlotsAusAufwand([
        (datum: DateTime(2026, 7, 26), stunden: 5),
        (datum: DateTime(2026, 7, 24), stunden: 3),
        (datum: DateTime(2026, 7, 25), stunden: 8),
      ]);
      expect(slots.map((s) => s.stunden).toList(), [3, 8, 5]);
      expect(slots.first.text, 'Fr 24.7.');
    });

    test('mehr als 5 Tage -> 5 Slots, Rest in "Weitere Tage", Summe erhalten', () {
      final zeilen = [
        for (var d = 1; d <= 6; d++)
          (datum: DateTime(2026, 8, d), stunden: 2.0),
      ];
      final slots = montageSlotsAusAufwand(zeilen);
      expect(slots.length, 5);
      expect(slots.last.text, 'Weitere Tage');
      // Tage 5 + 6 = 4 h im Sammel-Slot
      expect(slots.last.stunden, 4);
      final total = slots.fold<double>(0, (s, e) => s + e.stunden);
      expect(total, 12);
    });
  });
}
```

- [ ] **Step 2: Test ausführen — muss fehlschlagen**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter test test/core/util/event_aufwand_slots_test.dart`
Expected: FAIL (montageSlotsAusAufwand not defined).

- [ ] **Step 3: Util implementieren**

```dart
/// Ein Montage-Anlass-Slot: Freitext (wird als material_id gespeichert) + Stunden.
typedef MontageSlot = ({String text, double stunden});

const _wochentage = ['Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa', 'So'];

String _tagLabel(DateTime d) =>
    '${_wochentage[d.weekday - 1]} ${d.day}.${d.month}.';

/// Aggregiert Aufwand-Zeilen pro Tag zu Montage-Anlass-Slots (max 5).
/// [zeilen] = (datum, stunden). Ergebnis nach Datum aufsteigend. Bei > 5 Tagen
/// werden die ersten 4 Tage einzeln ausgewiesen und der Rest in einem
/// Sammel-Slot „Weitere Tage" summiert, damit die Gesamt-Stundenzahl stimmt.
List<MontageSlot> montageSlotsAusAufwand(
    List<({DateTime datum, double stunden})> zeilen) {
  // Pro Kalendertag summieren.
  final proTag = <DateTime, double>{};
  for (final z in zeilen) {
    final tag = DateTime(z.datum.year, z.datum.month, z.datum.day);
    proTag[tag] = (proTag[tag] ?? 0) + z.stunden;
  }
  final tage = proTag.keys.toList()..sort();
  if (tage.isEmpty) return [];

  if (tage.length <= 5) {
    return [for (final t in tage) (text: _tagLabel(t), stunden: proTag[t]!)];
  }

  final slots = <MontageSlot>[
    for (final t in tage.take(4)) (text: _tagLabel(t), stunden: proTag[t]!),
  ];
  var rest = 0.0;
  for (final t in tage.skip(4)) {
    rest += proTag[t]!;
  }
  slots.add((text: 'Weitere Tage', stunden: rest));
  return slots;
}
```

- [ ] **Step 4: Test ausführen — muss bestehen**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter test test/core/util/event_aufwand_slots_test.dart`
Expected: PASS (alle 4 Tests).

- [ ] **Step 5: Commit**

```bash
git add sbs_projer_app/lib/core/util/event_aufwand_slots.dart \
        sbs_projer_app/test/core/util/event_aufwand_slots_test.dart
git commit -m "feat(events): Aggregations-Util Aufwand->Montage-Slots mit Tests (E4)"
```

---

## Task 8: MontageFormScreen — optionaler `vorbefuellung`-Parameter

**Files:**
- Modify: `sbs_projer_app/lib/presentation/screens/montagen/montage_form_screen.dart`

Additive, rückwärtskompatible Erweiterung: neuer optionaler Parameter füllt Typ/Betrieb/Datum + Anlass-Slots vor. Bestehende Aufrufe (ohne Parameter) bleiben unverändert.

- [ ] **Step 1: Vorbefüllungs-Klasse + Konstruktor-Parameter**

Über der `class MontageFormScreen` (nach den Imports) eine kleine Wert-Klasse definieren:
```dart
/// Vorbefüllung für eine neue Anlass-Montage (aus dem Event-Zeit-Tab, E4).
class MontageVorbefuellung {
  final String montageTyp;
  final String? betriebId;
  final DateTime datum;
  final List<({String text, double stunden})> slots;
  const MontageVorbefuellung({
    required this.montageTyp,
    required this.betriebId,
    required this.datum,
    required this.slots,
  });
}
```

Im Widget die Felder + Konstruktor erweitern (bestehende Felder `montageId/anlageId/betriebId` unverändert lassen):
```dart
class MontageFormScreen extends ConsumerStatefulWidget {
  final String? montageId; // null = neu
  final String? anlageId;
  final String? betriebId;
  final MontageVorbefuellung? vorbefuellung;

  const MontageFormScreen({
    super.key,
    this.montageId,
    this.anlageId,
    this.betriebId,
    this.vorbefuellung,
  });
```

- [ ] **Step 2: Vorbefüllung in initState anwenden**

In `initState` direkt nach `_betriebId = widget.betriebId;` (aktuell Zeile 107) einfügen — vor `_loadStundensatz()`, damit der Stundensatz zum vorbefüllten `_datum` geladen wird:
```dart
    final vb = widget.vorbefuellung;
    if (vb != null && !_isEdit) {
      _montageTyp = vb.montageTyp;
      _datum = vb.datum;
      if (vb.betriebId != null) _betriebId = vb.betriebId;
      for (var i = 0; i < 5 && i < vb.slots.length; i++) {
        final s = vb.slots[i];
        // Bei Anlass ist die "material_id" der Freitext.
        _materialIds[i] = s.text;
        _materialControllers[i].text = s.text;
        _materialMengen[i] = s.stunden;
        _materialMengenControllers[i].text = s.stunden.toStringAsFixed(2);
      }
    }
```

- [ ] **Step 3: analyze**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter analyze lib/presentation/screens/montagen/montage_form_screen.dart`
Expected: keine neuen Fehler.

- [ ] **Step 4: Bestehende Montage-Erfassung schnell gegenprüfen (Regression)**

Sicherstellen, dass keine bestehende `MontageFormScreen(...)`-Instanziierung angepasst werden muss (neuer Parameter ist optional). Suche:
Run: `cd sbs_projer_app && grep -rn "MontageFormScreen(" lib | grep -v "class MontageFormScreen"`
Erwartet: alle Treffer kompilieren unverändert (kein Treffer erzwingt `vorbefuellung`).

- [ ] **Step 5: Commit**

```bash
git add sbs_projer_app/lib/presentation/screens/montagen/montage_form_screen.dart
git commit -m "feat(montage): optionaler vorbefuellung-Parameter fuer Anlass-Montage (E4)"
```

---

## Task 9: Event-Aufwand-Formular (`EventAufwandFormScreen`)

**Files:**
- Create: `sbs_projer_app/lib/presentation/screens/events/event_aufwand_form_screen.dart`

Vorlage: `event_einsatz_form_screen.dart` (Aufbau, Load/Save/Invalidate, dispose). Felder: Datum (Datepicker), Kategorie (Dropdown 4 Werte), Notiz (optional), Stunden (Dezimal, > 0). Kategorie-Labels: anfahrt→„Anfahrt", inbetriebnahme→„Inbetriebnahme", pikett→„Pikettdienst", spesen→„Spesen".

- [ ] **Step 1: Formular schreiben**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import 'package:sbs_projer_app/data/local/event_aufwand_local_export.dart';
import 'package:sbs_projer_app/data/repositories/event_aufwand_repository.dart';
import 'package:sbs_projer_app/presentation/providers/event_providers.dart';

const kAufwandKategorien = <String, String>{
  'anfahrt': 'Anfahrt',
  'inbetriebnahme': 'Inbetriebnahme',
  'pikett': 'Pikettdienst',
  'spesen': 'Spesen',
};

/// Formular zum Anlegen/Bearbeiten einer Zeit-/Spesen-Zeile (E4).
class EventAufwandFormScreen extends ConsumerStatefulWidget {
  final String eventId;
  final String? aufwandId; // null = neu

  const EventAufwandFormScreen({
    super.key,
    required this.eventId,
    this.aufwandId,
  });

  @override
  ConsumerState<EventAufwandFormScreen> createState() =>
      _EventAufwandFormScreenState();
}

class _EventAufwandFormScreenState
    extends ConsumerState<EventAufwandFormScreen> {
  final _formKey = GlobalKey<FormState>();
  bool _isLoading = false;
  bool _initialLoading = false;
  EventAufwandLocal? _existing;

  DateTime _datum = DateTime.now();
  String _kategorie = 'pikett';
  final _notizController = TextEditingController();
  final _stundenController = TextEditingController();

  bool get _isEdit => widget.aufwandId != null;

  @override
  void initState() {
    super.initState();
    if (_isEdit) {
      _initialLoading = true;
      _loadAufwand();
    }
  }

  Future<void> _loadAufwand() async {
    try {
      final list = await EventAufwandRepository.getByEvent(widget.eventId);
      final a = list
          .where((e) => e.routeId == widget.aufwandId)
          .cast<EventAufwandLocal?>()
          .firstWhere((e) => e != null, orElse: () => null);
      if (a == null || !mounted) {
        if (mounted) setState(() => _initialLoading = false);
        return;
      }
      setState(() {
        _existing = a;
        _datum = a.datum;
        _kategorie = a.kategorie;
        _notizController.text = a.notiz ?? '';
        _stundenController.text = a.stunden.toStringAsFixed(2);
        _initialLoading = false;
      });
    } catch (_) {
      if (mounted) setState(() => _initialLoading = false);
    }
  }

  Future<void> _datumWaehlen() async {
    final d = await showDatePicker(
      context: context,
      initialDate: _datum,
      firstDate: DateTime(2020),
      lastDate: DateTime(2100),
    );
    if (d != null && mounted) setState(() => _datum = d);
  }

  Future<void> _save() async {
    if (!_formKey.currentState!.validate()) return;
    setState(() => _isLoading = true);
    try {
      final a = _existing ?? EventAufwandLocal();
      if (!_isEdit) a.eventId = widget.eventId;
      a.datum = DateTime(_datum.year, _datum.month, _datum.day);
      a.kategorie = _kategorie;
      final notiz = _notizController.text.trim();
      a.notiz = notiz.isEmpty ? null : notiz;
      a.stunden = double.tryParse(_stundenController.text.replaceAll(',', '.')) ?? 0;
      await EventAufwandRepository.save(a);

      ref.invalidate(eventAufwaendeProvider(widget.eventId));
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text(_isEdit ? 'Zeit aktualisiert' : 'Zeit erfasst')),
        );
        context.pop();
      }
    } catch (e) {
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text('Fehler: $e')),
        );
      }
    } finally {
      if (mounted) setState(() => _isLoading = false);
    }
  }

  @override
  void dispose() {
    _notizController.dispose();
    _stundenController.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    if (_initialLoading) {
      return const Scaffold(body: Center(child: CircularProgressIndicator()));
    }
    return Scaffold(
      appBar: AppBar(title: Text(_isEdit ? 'Zeit bearbeiten' : 'Zeit erfassen')),
      body: Form(
        key: _formKey,
        child: ListView(
          padding: const EdgeInsets.all(16),
          children: [
            InkWell(
              onTap: _datumWaehlen,
              borderRadius: BorderRadius.circular(4),
              child: InputDecorator(
                decoration: const InputDecoration(
                  labelText: 'Datum',
                  prefixIcon: Icon(Icons.event),
                ),
                child: Text(_ddMMyyyy(_datum)),
              ),
            ),
            const SizedBox(height: 16),
            DropdownButtonFormField<String>(
              initialValue: _kategorie,
              decoration: const InputDecoration(
                labelText: 'Kategorie *',
                prefixIcon: Icon(Icons.category),
              ),
              items: [
                for (final e in kAufwandKategorien.entries)
                  DropdownMenuItem(value: e.key, child: Text(e.value)),
              ],
              onChanged: (v) {
                if (v != null) setState(() => _kategorie = v);
              },
            ),
            const SizedBox(height: 16),
            TextFormField(
              controller: _notizController,
              decoration: const InputDecoration(
                labelText: 'Notiz',
                prefixIcon: Icon(Icons.note),
                hintText: 'z.B. 10:00–02:00 oder Essen CHF 120',
              ),
              textInputAction: TextInputAction.next,
            ),
            const SizedBox(height: 16),
            TextFormField(
              controller: _stundenController,
              decoration: const InputDecoration(
                labelText: 'Stunden *',
                prefixIcon: Icon(Icons.schedule),
                suffixText: 'h',
              ),
              keyboardType: const TextInputType.numberWithOptions(decimal: true),
              validator: (v) {
                final n = double.tryParse((v ?? '').replaceAll(',', '.'));
                if (n == null || n <= 0) return 'Stunden > 0 erforderlich';
                return null;
              },
            ),
            const SizedBox(height: 24),
            FilledButton(
              onPressed: _isLoading ? null : _save,
              child: _isLoading
                  ? const SizedBox(
                      height: 20, width: 20,
                      child: CircularProgressIndicator(strokeWidth: 2))
                  : Text(_isEdit ? 'Speichern' : 'Zeit erfassen'),
            ),
            const SizedBox(height: 32),
          ],
        ),
      ),
    );
  }
}

String _zwei(int v) => v.toString().padLeft(2, '0');
String _ddMMyyyy(DateTime d) => '${_zwei(d.day)}.${_zwei(d.month)}.${d.year}';
```

- [ ] **Step 2: analyze**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter analyze lib/presentation/screens/events/event_aufwand_form_screen.dart`
Expected: keine Fehler.

- [ ] **Step 3: Commit**

```bash
git add sbs_projer_app/lib/presentation/screens/events/event_aufwand_form_screen.dart
git commit -m "feat(events): EventAufwand-Formular (E4)"
```

---

## Task 10: 5. Tab „Zeit" im Event-Detail + „Montage generieren"

**Files:**
- Modify: `sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart`

Neuer Tab-Index 3 = „Zeit"; „Dokumente" rückt auf Index 4. TabController-Länge 4→5, `TabBar` scrollbar. FAB-`switch` erweitern. Neuer `_ZeitTab` mit Total-Chip, „Montage generieren"-Button und Aufwand-Liste (Bearbeiten/Löschen).

- [ ] **Step 1: Imports ergänzen**

Bei den bestehenden Imports von `event_detail_screen.dart`:
```dart
import 'package:sbs_projer_app/core/util/event_aufwand_slots.dart';
import 'package:sbs_projer_app/data/local/event_aufwand_local_export.dart';
import 'package:sbs_projer_app/data/repositories/event_aufwand_repository.dart';
import 'package:sbs_projer_app/presentation/screens/events/event_aufwand_form_screen.dart';
import 'package:sbs_projer_app/presentation/screens/montagen/montage_form_screen.dart';
```

- [ ] **Step 2: TabController-Länge auf 5**

`event_detail_screen.dart:54`:
```dart
    _tabController = TabController(length: 5, vsync: this);
```

- [ ] **Step 3: TabBar + TabBarView erweitern**

`event_detail_screen.dart:240-258` — `TabBar` scrollbar machen und „Zeit" vor „Dokumente" einfügen:
```dart
              TabBar(
                controller: _tabController,
                isScrollable: true,
                tabs: const [
                  Tab(text: 'Kontakte'),
                  Tab(text: 'Stände'),
                  Tab(text: 'Einsätze'),
                  Tab(text: 'Zeit'),
                  Tab(text: 'Dokumente'),
                ],
              ),
              Expanded(
                child: TabBarView(
                  controller: _tabController,
                  children: [
                    _KontakteTab(eventServerId: eventServerId),
                    _StaendeTab(event: event),
                    _EinsaetzeTab(event: event),
                    _ZeitTab(event: event),
                    _DokumenteTab(eventServerId: eventServerId),
                  ],
                ),
              ),
```

- [ ] **Step 4: FAB-`switch` erweitern**

In `_buildFab` (`event_detail_screen.dart:269`) den bisherigen `case 3` (Dokument) auf `case 4` umnummerieren und neuen `case 3` (Zeit) einfügen:
```dart
      case 3:
        return FloatingActionButton.extended(
          onPressed: () => _zeitErfassen(eventServerId),
          icon: const Icon(Icons.schedule),
          label: const Text('Zeit erfassen'),
        );
      case 4:
        return FloatingActionButton.extended(
          onPressed: () => _dokumentHochladen(eventServerId),
          icon: const Icon(Icons.upload_file),
          label: const Text('Dokument hochladen'),
        );
```
Den Doc-Kommentar über `_buildFab` (Zeile 267-268) entsprechend anpassen: „0 Kontakt zuordnen, 1 Stand hinzufügen, 2 Einsatz erfassen, 3 Zeit erfassen, 4 Dokument hochladen."

- [ ] **Step 5: `_zeitErfassen`-Handler (neu) neben `_einsatzHinzufuegen`**

Nach `_einsatzHinzufuegen` (um Zeile 308) einfügen:
```dart
  /// Öffnet das Zeit-Formular (neu) und aktualisiert danach die Liste.
  Future<void> _zeitErfassen(String eventServerId) async {
    await Navigator.of(context).push(
      MaterialPageRoute<void>(
        builder: (_) => EventAufwandFormScreen(eventId: eventServerId),
      ),
    );
    ref.invalidate(eventAufwaendeProvider(eventServerId));
  }
```

- [ ] **Step 6: `_ZeitTab`-Widget implementieren**

Neben `_EinsaetzeTab` (nach dessen Klasse, um Zeile 1160) einfügen. Enthält: Kopfzeile mit Total-Stunden-Chip + „Montage generieren"-Button; Liste der Aufwand-Zeilen (Datum, Kategorie-Label, Notiz, Stunden) mit Tap→Bearbeiten und Löschen-Icon.
```dart
class _ZeitTab extends ConsumerWidget {
  final EventLocal event;
  const _ZeitTab({required this.event});

  Future<void> _bearbeiten(
      BuildContext context, WidgetRef ref, EventAufwandLocal a) async {
    await Navigator.of(context).push(
      MaterialPageRoute<void>(
        builder: (_) => EventAufwandFormScreen(
          eventId: event.serverId!,
          aufwandId: a.routeId,
        ),
      ),
    );
    ref.invalidate(eventAufwaendeProvider(event.serverId!));
  }

  Future<void> _loeschen(
      BuildContext context, WidgetRef ref, EventAufwandLocal a) async {
    final ok = await showDialog<bool>(
      context: context,
      builder: (ctx) => AlertDialog(
        title: const Text('Zeit löschen'),
        content: const Text('Diese Zeile wirklich löschen?'),
        actions: [
          TextButton(
              onPressed: () => Navigator.pop(ctx, false),
              child: const Text('Abbrechen')),
          FilledButton(
            onPressed: () => Navigator.pop(ctx, true),
            style: FilledButton.styleFrom(backgroundColor: AppColors.error),
            child: const Text('Löschen'),
          ),
        ],
      ),
    );
    if (ok != true) return;
    try {
      await EventAufwandRepository.delete(a.routeId);
      ref.invalidate(eventAufwaendeProvider(event.serverId!));
      if (context.mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(content: Text('Zeit gelöscht')),
        );
      }
    } catch (e) {
      if (context.mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text('Fehler: $e')),
        );
      }
    }
  }

  Future<void> _montageGenerieren(
      BuildContext context, List<EventAufwandLocal> zeilen) async {
    final slots = montageSlotsAusAufwand(
      [for (final a in zeilen) (datum: a.datum, stunden: a.stunden)],
    );
    await Navigator.of(context).push(
      MaterialPageRoute<void>(
        builder: (_) => MontageFormScreen(
          vorbefuellung: MontageVorbefuellung(
            montageTyp: 'anlass',
            betriebId: event.betriebId,
            datum: event.terminVon ?? DateTime.now(),
            slots: slots,
          ),
        ),
      ),
    );
  }

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final async = ref.watch(eventAufwaendeProvider(event.serverId!));
    return async.when(
      loading: () => const Center(child: CircularProgressIndicator()),
      error: (e, _) => Center(child: Text('Fehler: $e')),
      data: (zeilen) {
        final total = zeilen.fold<double>(0, (s, a) => s + a.stunden);
        return Column(
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            Padding(
              padding: const EdgeInsets.fromLTRB(16, 12, 16, 4),
              child: Row(
                children: [
                  Chip(
                    label: Text('Total ${total.toStringAsFixed(2)} h'),
                    backgroundColor: AppColors.surface,
                  ),
                  const Spacer(),
                  TextButton.icon(
                    onPressed: zeilen.isEmpty
                        ? null
                        : () => _montageGenerieren(context, zeilen),
                    icon: const Icon(Icons.receipt_long, size: 18),
                    label: const Text('Montage generieren'),
                  ),
                ],
              ),
            ),
            if (zeilen.isEmpty)
              const Expanded(
                child: Center(
                  child: Padding(
                    padding: EdgeInsets.all(32),
                    child: Text('Noch keine Zeiten erfasst.',
                        style: TextStyle(color: AppColors.textSecondary)),
                  ),
                ),
              )
            else
              Expanded(
                child: ListView.builder(
                  padding: const EdgeInsets.fromLTRB(16, 4, 16, 88),
                  itemCount: zeilen.length,
                  itemBuilder: (ctx, i) {
                    final a = zeilen[i];
                    return Card(
                      margin: const EdgeInsets.only(bottom: 8),
                      child: ListTile(
                        onTap: () => _bearbeiten(context, ref, a),
                        title: Text(
                          kAufwandKategorien[a.kategorie] ?? a.kategorie,
                          style: const TextStyle(
                              fontSize: 14, fontWeight: FontWeight.w600),
                        ),
                        subtitle: Text(
                          '${_ddMMyyyy(a.datum)}'
                          '${(a.notiz != null && a.notiz!.isNotEmpty) ? ' · ${a.notiz}' : ''}',
                          style: const TextStyle(
                              fontSize: 12, color: AppColors.textSecondary),
                        ),
                        trailing: Row(
                          mainAxisSize: MainAxisSize.min,
                          children: [
                            Text('${a.stunden.toStringAsFixed(2)} h',
                                style: const TextStyle(
                                    fontWeight: FontWeight.w600)),
                            IconButton(
                              icon: const Icon(Icons.delete_outline,
                                  color: AppColors.error),
                              onPressed: () => _loeschen(context, ref, a),
                            ),
                          ],
                        ),
                      ),
                    );
                  },
                ),
              ),
          ],
        );
      },
    );
  }
}
```

- [ ] **Step 7: Datums-Helfer verfügbar machen**

`_ZeitTab` nutzt `_ddMMyyyy` und `kAufwandKategorien`. `kAufwandKategorien` kommt aus dem Formular-Import (Task 9, Top-Level `const`). Für `_ddMMyyyy`: prüfen, ob in `event_detail_screen.dart` bereits ein passender Datums-Helfer existiert (z. B. `_ddMMHHmm`); falls kein `dd.MM.yyyy`-Helfer da ist, unten in der Datei ergänzen:
```dart
String _ddMMyyyy(DateTime d) =>
    '${d.day.toString().padLeft(2, '0')}.${d.month.toString().padLeft(2, '0')}.${d.year}';
```
(Wenn ein gleichnamiger Helfer schon existiert, diesen Schritt überspringen — keine Duplikate anlegen.)

- [ ] **Step 8: analyze**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter analyze lib/presentation/screens/events/event_detail_screen.dart`
Expected: keine neuen Fehler.

- [ ] **Step 9: Commit**

```bash
git add sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): Zeit-Tab + Montage-generieren im Event-Detail (E4)"
```

---

## Task 11: Gesamtverifikation, visueller Test, Deploy (v0.20.0)

**Files:**
- Modify: `sbs_projer_app/pubspec.yaml` (Version)
- Modify: `ToDo.md`, `Projekt.md` (Doku)

- [ ] **Step 1: Volle Test-Suite + analyze**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && flutter analyze && flutter test`
Expected: analyze ohne neue Fehler; alle Tests grün (inkl. der neuen aus Task 2 + 7).

- [ ] **Step 2: Web-Build für visuellen Test**

Run: `cd sbs_projer_app && export PATH="$PATH:/c/flutter/bin" && export MSYS_NO_PATHCONV=1 && flutter build web --base-href "/sbs-projer-dev/" --pwa-strategy=none`
Expected: „Built build\web".

- [ ] **Step 3: Visueller Browser-Test (Pflicht — siehe Memory `feedback_ui_vor_deploy_testen`)**

Lokal servieren und via Browser prüfen:
- Event öffnen → 5 Tabs sichtbar (scrollbar), „Zeit" zwischen Einsätze und Dokumente.
- „Zeit erfassen": Datum/Kategorie/Notiz/Stunden speichern → erscheint in Liste (ggf. nach Reload, Web-Refetch-Latenz), Total-Chip stimmt. Bearbeiten + Löschen.
- Mehrere Zeilen an verschiedenen Tagen → „Montage generieren" → Montage-Formular öffnet mit Typ **Anlass**, Betrieb = Veranstaltungs-Betrieb, Slots **pro Tag** mit Tages-Summe, Stundensatz aus Preisliste; speicherbar.
- Andere Tabs (Kontakte/Stände/Einsätze/Dokumente) unverändert.
- Regressions-Check: eine normale Montage (nicht aus Event) neu anlegen → Formular unverändert.

- [ ] **Step 4: Version bumpen**

`sbs_projer_app/pubspec.yaml:4`:
```yaml
version: 0.20.0+483
```

- [ ] **Step 5: Doku aktualisieren**

- `ToDo.md`: neuen Abschnitt „Events-Modul — Phase E4 (live v0.20.0)" mit den erledigten Punkten + offenem Handy-Check-Punkt; Live-Version im Header auf v0.20.0. Nächste Phase „E5 Abschluss-Mail".
- `Projekt.md`: Header-Version auf `0.20.0+483`, neue „Zuletzt"-Zeile für E4.

- [ ] **Step 6: Commit + Merge + Deploy**

```bash
git add sbs_projer_app/pubspec.yaml ToDo.md Projekt.md
git commit -m "chore: Version 0.20.0+483 + Doku (Events E4)"
```
Danach Deploy-Workflow gemäß CLAUDE.md: Feature-Branch nach `main` mergen + pushen, Web-Build + Cache-Bust (`main.dart.js?v=0.20.0` in `flutter_bootstrap.js`, `flutter_service_worker.js` löschen), auf `gh-pages` kopieren + pushen, zurück auf `main`.

---

## Self-Review (durchgeführt)

**1. Spec-Abdeckung:**
- Datenmodell `event_aufwand` (Migration 123) → Task 1; DTO/Local/Web/Export/Mapper/IsarService/Repo/Provider/Sync → Tasks 2–6. ✓
- Erfassungs-UI 5. Tab „Zeit" + Formular (Datum/Kategorie/Notiz/Stunden) → Tasks 9–10. ✓
- Auto-Montage: Aggregation pro Tag (≤5 Slots, >5-Tage-Fall) → Task 7; vorbefülltes MontageFormScreen → Tasks 8 + 10. ✓
- Spesen = Kategorie mit Stunden (kein CHF-Feld) → im DTO/Formular-Kategorien abgebildet. ✓
- Tests (Aggregation, DTO), analyze, build_runner, visueller Test, Deploy v0.20.0 → Tasks 2, 7, 11. ✓

**2. Platzhalter-Scan:** keine TBD/TODO/„handle edge cases"; alle Code-Schritte vollständig. ✓

**3. Typ-Konsistenz:** `EventAufwand`/`EventAufwandLocal`/`EventAufwandMapper`/`EventAufwandRepository`/`eventAufwaendeProvider`/`eventAufwand*`-IsarService-Methoden konsistent. `MontageSlot = ({String text, double stunden})` in Util (Task 7) und `MontageVorbefuellung.slots` (Task 8) sowie der Aufruf in Task 10 verwenden identisch `(text, stunden)`. `montageSlotsAusAufwand` nimmt `({DateTime datum, double stunden})` — in Task 10 exakt so aufgerufen. `kAufwandKategorien` in Task 9 definiert, in Task 10 genutzt. ✓
