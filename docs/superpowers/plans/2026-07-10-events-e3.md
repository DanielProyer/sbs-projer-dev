# Events-Modul Phase E3 — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Inbetriebnahme pro Anlage, GPS-Standort pro Stand, Karten-Umschalter (swisstopo-Luftbild) im Stände-Tab und ein neuer Einsätze-Tab mit minimalem Pikett-Formular — gemäss Spec `docs/superpowers/specs/2026-07-10-events-e3-design.md`.

**Architecture:** Bestehende Stand-/Anlage-Vertikalen um Felder erweitern (Stand: latitude/longitude; Anlage: inBetrieb/inBetriebAm). Neue Sync-Vertikale `event_einsaetze` nach E1/E2-Muster. Karte via `flutter_map` + swisstopo-WMTS (kein Key), GPS via `geolocator`. Inbetriebnahme wird live in der Stände-Ansicht getoggelt; das Stand-Formular speichert Anlagen id-basiert, damit `inBetrieb` beim Bearbeiten erhalten bleibt.

**Tech Stack:** Flutter/Dart ^3.11, Riverpod, Isar (Conditional Exports), Supabase (MCP `apply_migration`/`execute_sql`, Project `pltbaqqwpnmdajwgnhpd`), flutter_map, latlong2, geolocator, uuid.

**Projektregeln:**
- Shell Git Bash, `export PATH="$PATH:/c/flutter/bin"`, App-Verzeichnis `sbs_projer_app`. build_runner Timeout 600000.
- Local-Imports über `*_export.dart` (Ausnahme: `isar_service.dart`, `sync_service.dart` importieren native Locals direkt).
- KEINE `?ausdruck`-Collection-Elemente (bricht build_runner). Immer `if (x != null) …`.
- `.g.dart` ist gitignored — nie stagen. Kommentare/Labels Deutsch.
- Nach jedem Task `flutter analyze` ohne neue errors/warnings ausserhalb `.g.dart` (Baseline 47 infos), dann committen.
- **Feldnamen:** Dart camelCase (`inBetrieb`, `inBetriebAm`, `latitude`, `longitude`), DB snake_case (`in_betrieb`, `in_betrieb_am`, `latitude`, `longitude`).
- **Kopiervorlage** (vorher lesen): `event_stand`-Vertikale + `event_einsaetze` baut nach `event_dokument`-Vertikale (E2). Sync-Vorlage `_syncEventStaende` in `sync_service.dart`.

---

### Task 1: Dependencies + Web-Berechtigung

**Files:** Modify `sbs_projer_app/pubspec.yaml`, `sbs_projer_app/web/index.html`

- [ ] **Step 1: Packages hinzufügen** (Versionen automatisch auflösen)

Run: `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && flutter pub add flutter_map latlong2 geolocator`
Expected: `Changed 3 dependencies!` (oder mehr inkl. transitiver). Danach in `pubspec.yaml` prüfen, dass die drei unter `dependencies:` stehen; einen Kommentar `# Karte & GPS (E3)` davor setzen (kosmetisch).

- [ ] **Step 2: Permissions-Policy in web/index.html**

Im `<head>` nach `<link rel="manifest" href="manifest.json">` einfügen:

```html
  <!-- E3: Browser-Geolocation für Stand-GPS (nur self) -->
  <meta http-equiv="Permissions-Policy" content="geolocation=(self)">
```

- [ ] **Step 3: pub get + Analyze**

Run: `flutter pub get && flutter analyze`
Expected: keine neuen errors/warnings (transitive Infos aus den neuen Paketen ignorieren, sofern keine errors).

- [ ] **Step 4: Commit**

```bash
git add sbs_projer_app/pubspec.yaml sbs_projer_app/pubspec.lock sbs_projer_app/web/index.html
git commit -m "feat(events): flutter_map + geolocator Dependencies + Web-Geolocation (E3)"
```

---

### Task 2: Migration 122 — Felder + event_einsaetze

**Files:** Create `Datenbank/migrations/122_events_e3.sql`

- [ ] **Step 1: Migration schreiben**

```sql
-- ============================================================
-- Migration 122: Events E3 — GPS, Inbetriebnahme, Einsaetze
-- Projekt: SBS Projer App
-- Stand: 10.07.2026
-- ============================================================

-- Stand-GPS
ALTER TABLE event_staende
  ADD COLUMN IF NOT EXISTS latitude double precision,
  ADD COLUMN IF NOT EXISTS longitude double precision;

-- Inbetriebnahme pro Anlage
ALTER TABLE event_stand_anlagen
  ADD COLUMN IF NOT EXISTS in_betrieb boolean NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS in_betrieb_am timestamptz;

-- Pikett-Einsaetze
CREATE TABLE event_einsaetze (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  event_id uuid NOT NULL REFERENCES events(id) ON DELETE CASCADE,
  stand_id uuid REFERENCES event_staende(id) ON DELETE SET NULL,
  zeitpunkt timestamptz NOT NULL DEFAULT now(),
  beschreibung text NOT NULL,
  material text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
ALTER TABLE event_einsaetze ENABLE ROW LEVEL SECURITY;
CREATE POLICY event_einsaetze_user_policy ON event_einsaetze
  FOR ALL USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
CREATE TRIGGER update_event_einsaetze_updated_at BEFORE UPDATE ON event_einsaetze
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE INDEX idx_event_einsaetze_event ON event_einsaetze(user_id, event_id);
```

- [ ] **Step 2: Anwenden + verifizieren** (Supabase MCP `apply_migration`, name `122_events_e3`).
`execute_sql`: `SELECT column_name FROM information_schema.columns WHERE table_name='event_staende' AND column_name IN ('latitude','longitude');` → 2; `… table_name='event_stand_anlagen' AND column_name IN ('in_betrieb','in_betrieb_am');` → 2; `SELECT table_name FROM information_schema.tables WHERE table_name='event_einsaetze';` → 1.

- [ ] **Step 3: Commit**

```bash
git add Datenbank/migrations/122_events_e3.sql
git commit -m "feat(events): Migration 122 GPS/Inbetriebnahme/Einsaetze (E3)"
```

---

### Task 3: Stand + Anlage um neue Felder erweitern

**Files:** Modify `event_stand.dart`, `event_stand_local.dart`, `web/event_stand_local_web.dart`, `event_stand_mapper.dart`, `event_stand_anlage.dart`, `event_stand_anlage_local.dart`, `web/event_stand_anlage_local_web.dart`, `event_stand_anlage_mapper.dart`, `event_stand_repository.dart` (uebernehmeVon)

- [ ] **Step 1: EventStand DTO** — `latitude`/`longitude` ergänzen:

In `data/models/event_stand.dart`: Felder nach `notizen` → `final double? latitude; final double? longitude;`. Konstruktor: `this.latitude, this.longitude,`. In `fromJson`: `latitude: (json['latitude'] as num?)?.toDouble(), longitude: (json['longitude'] as num?)?.toDouble(),`. In `toJson` NACH `'notizen': notizen,`: `'latitude': latitude, 'longitude': longitude,`.

- [ ] **Step 2: EventStandLocal + Web-Stub** — nach `String? notizen;` je `double? latitude; double? longitude;` in `event_stand_local.dart` UND `web/event_stand_local_web.dart`.

- [ ] **Step 3: EventStandMapper** — in `fromDto` nach `local.notizen = dto.notizen;`: `local.latitude = dto.latitude; local.longitude = dto.longitude;`. In `toJson` nach `'notizen': local.notizen,`: `'latitude': local.latitude, 'longitude': local.longitude,`.

- [ ] **Step 4: EventStandAnlage DTO** — `inBetrieb`/`inBetriebAm` ergänzen:

In `data/models/event_stand_anlage.dart`: Felder nach `sortierung` → `final bool inBetrieb; final DateTime? inBetriebAm;`. Konstruktor: `this.inBetrieb = false, this.inBetriebAm,`. `fromJson`: `inBetrieb: json['in_betrieb'] ?? false, inBetriebAm: json['in_betrieb_am'] != null ? DateTime.parse(json['in_betrieb_am']) : null,`. `toJson` nach `'sortierung': sortierung,`: `'in_betrieb': inBetrieb, 'in_betrieb_am': inBetriebAm?.toIso8601String(),`.

- [ ] **Step 5: EventStandAnlageLocal + Web-Stub** — nach `int sortierung = 0;` je `bool inBetrieb = false; DateTime? inBetriebAm;` in `event_stand_anlage_local.dart` UND `web/event_stand_anlage_local_web.dart`.

- [ ] **Step 6: EventStandAnlageMapper** — in `fromDto` nach `local.sortierung = dto.sortierung;`: `local.inBetrieb = dto.inBetrieb; local.inBetriebAm = dto.inBetriebAm;`. In `toJson` nach `'sortierung': local.sortierung,`: `'in_betrieb': local.inBetrieb, 'in_betrieb_am': local.inBetriebAm?.toIso8601String(),`.

- [ ] **Step 7: uebernehmeVon** — in `event_stand_repository.dart` beim Kopieren des Stands nach `..notizen = q.notizen;`: `..latitude = q.latitude ..longitude = q.longitude` (Achtung Cascade-Syntax: eigene Zeilen `..latitude = q.latitude` etc.). Beim Kopieren der Anlage nach `..sortierung = qa.sortierung;`: `..inBetrieb = qa.inBetrieb ..inBetriebAm = qa.inBetriebAm`.

- [ ] **Step 8: build_runner + Analyze**

Run: `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && dart run build_runner build --delete-conflicting-outputs` → `Succeeded`; `flutter analyze` keine neuen Findings.

- [ ] **Step 9: Commit** (`.g.dart` NICHT stagen)

```bash
git add sbs_projer_app/lib/data/models/event_stand.dart sbs_projer_app/lib/data/local/event_stand_local.dart sbs_projer_app/lib/data/local/web/event_stand_local_web.dart sbs_projer_app/lib/data/mappers/event_stand_mapper.dart sbs_projer_app/lib/data/models/event_stand_anlage.dart sbs_projer_app/lib/data/local/event_stand_anlage_local.dart sbs_projer_app/lib/data/local/web/event_stand_anlage_local_web.dart sbs_projer_app/lib/data/mappers/event_stand_anlage_mapper.dart sbs_projer_app/lib/data/repositories/event_stand_repository.dart
git commit -m "feat(events): Stand-GPS + Anlage-Inbetriebnahme Felder (E3)"
```

---

### Task 4: EventEinsatz-Vertikale (DTO, Local, Mapper, IsarService, Repository, Provider)

**Files:** Create `event_einsatz.dart`, `event_einsatz_local.dart`, `event_einsatz_local_export.dart`, `web/event_einsatz_local_web.dart`, `event_einsatz_mapper.dart`, `event_einsatz_repository.dart`; Modify `isar_service.dart`, `isar_service_web.dart`, `event_providers.dart`

- [ ] **Step 1: DTO** `data/models/event_einsatz.dart`:

```dart
/// Supabase-DTO für einen Pikett-Einsatz während eines Events.
class EventEinsatz {
  final String id;
  final String userId;
  final String eventId;
  final String? standId;
  final DateTime zeitpunkt;
  final String beschreibung;
  final String? material;
  final DateTime? createdAt;
  final DateTime? updatedAt;

  EventEinsatz({
    required this.id,
    required this.userId,
    required this.eventId,
    this.standId,
    required this.zeitpunkt,
    required this.beschreibung,
    this.material,
    this.createdAt,
    this.updatedAt,
  });

  factory EventEinsatz.fromJson(Map<String, dynamic> json) {
    return EventEinsatz(
      id: json['id'],
      userId: json['user_id'],
      eventId: json['event_id'],
      standId: json['stand_id'],
      zeitpunkt: DateTime.parse(json['zeitpunkt']),
      beschreibung: json['beschreibung'],
      material: json['material'],
      createdAt: json['created_at'] != null ? DateTime.parse(json['created_at']) : null,
      updatedAt: json['updated_at'] != null ? DateTime.parse(json['updated_at']) : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'user_id': userId,
      'event_id': eventId,
      'stand_id': standId,
      'zeitpunkt': zeitpunkt.toIso8601String(),
      'beschreibung': beschreibung,
      'material': material,
    };
  }
}
```

- [ ] **Step 2: Isar-Local** `data/local/event_einsatz_local.dart` (Struktur wie `event_dokument_local.dart`):

```dart
import 'package:flutter/foundation.dart' show kIsWeb;
import 'package:isar/isar.dart';

part 'event_einsatz_local.g.dart';

@collection
class EventEinsatzLocal {
  Id id = Isar.autoIncrement;

  @ignore
  String get routeId => kIsWeb ? serverId! : id.toString();

  // Supabase Sync
  @Index()
  String? serverId;
  @Index()
  bool isSynced = false;
  DateTime? lastModifiedAt;

  // Felder
  late String userId;
  @Index()
  late String eventId;
  String? standId;
  late DateTime zeitpunkt;
  late String beschreibung;
  String? material;
  DateTime? createdAt;
  DateTime? updatedAt;
}
```

- [ ] **Step 3: Web-Stub** `data/local/web/event_einsatz_local_web.dart`:

```dart
/// Web-Stub für EventEinsatzLocal (kein Isar auf Web).
class EventEinsatzLocal {
  int id = 0;

  String get routeId => serverId!;

  // Supabase Sync
  String? serverId;
  bool isSynced = false;
  DateTime? lastModifiedAt;

  // Felder
  String userId = '';
  String eventId = '';
  String? standId;
  DateTime zeitpunkt = DateTime(2000);
  String beschreibung = '';
  String? material;
  DateTime? createdAt;
  DateTime? updatedAt;
}
```

- [ ] **Step 4: Export** `data/local/event_einsatz_local_export.dart`:
```dart
export 'event_einsatz_local.dart' if (dart.library.html) 'web/event_einsatz_local_web.dart';
```

- [ ] **Step 5: Mapper** `data/mappers/event_einsatz_mapper.dart` (Muster `event_dokument_mapper.dart`): `fromDto` setzt alle Felder + `isSynced=true` + `lastModifiedAt = dto.updatedAt ?? dto.createdAt ?? DateTime.now()`; `toJson` gibt `user_id/event_id/stand_id/zeitpunkt(ISO)/beschreibung/material` und `id` wenn serverId gesetzt.

- [ ] **Step 6: IsarService** — Import + `EventEinsatzLocalSchema` in Schema-Liste + Methoden nach dem EventDokument-Block:

```dart
  // ─── EventEinsatz ───
  static Future<List<EventEinsatzLocal>> eventEinsatzFindByEvent(String eventId) =>
      instance.eventEinsatzLocals.filter().eventIdEqualTo(eventId).findAll();
  static Future<EventEinsatzLocal?> eventEinsatzGet(int id) =>
      instance.eventEinsatzLocals.get(id);
  static Future<EventEinsatzLocal?> eventEinsatzFindByServerId(String serverId) =>
      instance.eventEinsatzLocals.filter().serverIdEqualTo(serverId).findFirst();
  static Future<void> eventEinsatzPut(EventEinsatzLocal e) =>
      instance.writeTxn(() => instance.eventEinsatzLocals.put(e));
  static Future<void> eventEinsatzDelete(int id) =>
      instance.writeTxn(() => instance.eventEinsatzLocals.delete(id));
```

In `isar_service_web.dart` 5 analoge `UnsupportedError`-Stubs.

- [ ] **Step 7: Repository** `data/repositories/event_einsatz_repository.dart` (Muster `event_dokument_repository.dart`):
- `getByEvent(String eventId)`: Web `from('event_einsaetze').select().eq('user_id',_userId).eq('event_id',eventId).order('zeitpunkt', ascending: false)` → Mapper; Native `eventEinsatzFindByEvent` (Sortierung im UI).
- `save(EventEinsatzLocal e)`: `e.userId=_userId; e.serverId ??= Uuid().v4();` Web upsert; Native `isSynced=false; lastModifiedAt=now(); eventEinsatzPut`.
- `delete(String id)`: Web delete eq id; Native serverseitig (via `eventEinsatzGet`→serverId) dann `eventEinsatzDelete` (E1-W1-Muster).

- [ ] **Step 8: Provider** — in `event_providers.dart` ergänzen:
```dart
final eventEinsaetzeProvider =
    FutureProvider.family<List<EventEinsatzLocal>, String>((ref, eventId) async {
  final list = await EventEinsatzRepository.getByEvent(eventId);
  list.sort((a, b) => b.zeitpunkt.compareTo(a.zeitpunkt)); // neueste zuerst
  return list;
});
```
(Import `event_einsatz_local_export.dart` + Repository ergänzen.)

- [ ] **Step 9: build_runner + Analyze + Commit**

Run: `dart run build_runner build --delete-conflicting-outputs` → Succeeded; `flutter analyze` sauber.
```bash
git add sbs_projer_app/lib/data/models/event_einsatz.dart sbs_projer_app/lib/data/local/event_einsatz_local.dart sbs_projer_app/lib/data/local/event_einsatz_local_export.dart sbs_projer_app/lib/data/local/web/event_einsatz_local_web.dart sbs_projer_app/lib/data/mappers/event_einsatz_mapper.dart sbs_projer_app/lib/data/repositories/event_einsatz_repository.dart sbs_projer_app/lib/services/storage/isar_service.dart sbs_projer_app/lib/services/storage/isar_service_web.dart sbs_projer_app/lib/presentation/providers/event_providers.dart
git commit -m "feat(events): EventEinsatz-Vertikale (DTO/Local/Mapper/Repo/Provider) (E3)"
```

---

### Task 5: Sync-Integration event_einsaetze

**Files:** Modify `services/sync/sync_service.dart`

- [ ] **Step 1:** Methode `_syncEventEinsaetze` exakt nach `_syncEventStaende` (Tabelle `event_einsaetze`, `EventEinsatzMapper`, Isar `eventEinsatzLocals`, Meta-Key `event_einsaetze`, LWW identisch).

- [ ] **Step 2:** In **Tier 3** einhängen (neben `_syncEventStaende` — braucht events; stand_id ist nullable/SET NULL, daher kein harter FK-Zwang zu Tier 4).

- [ ] **Step 3: Analyze + Commit**
```bash
git add sbs_projer_app/lib/services/sync/sync_service.dart
git commit -m "feat(events): Sync-Integration event_einsaetze (E3)"
```

---

### Task 6: Inbetriebnahme-Fortschritt Util (TDD)

**Files:** Create `lib/core/util/inbetriebnahme.dart`, Test `test/core/util/inbetriebnahme_test.dart`

- [ ] **Step 1: Failing Test**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:sbs_projer_app/core/util/inbetriebnahme.dart';

void main() {
  group('inbetriebnahmeFortschritt', () {
    test('keine Anlagen -> 0/0, nicht komplett', () {
      final f = inbetriebnahmeFortschritt([]);
      expect(f.total, 0);
      expect(f.inBetrieb, 0);
      expect(f.komplett, isFalse);
    });
    test('summiert anzahl, nur in-Betrieb zaehlen', () {
      final f = inbetriebnahmeFortschritt([
        (anzahl: 7, inBetrieb: true),
        (anzahl: 3, inBetrieb: false),
        (anzahl: 2, inBetrieb: true),
      ]);
      expect(f.total, 12);
      expect(f.inBetrieb, 9);
      expect(f.komplett, isFalse);
    });
    test('alle in Betrieb -> komplett', () {
      final f = inbetriebnahmeFortschritt([
        (anzahl: 4, inBetrieb: true),
        (anzahl: 1, inBetrieb: true),
      ]);
      expect(f.komplett, isTrue);
      expect(f.label, '✓ komplett');
    });
    test('label X/Y', () {
      expect(inbetriebnahmeFortschritt([(anzahl: 5, inBetrieb: true), (anzahl: 5, inBetrieb: false)]).label, '5/10 in Betrieb');
    });
  });
}
```

- [ ] **Step 2: Run — FAIL**

Run: `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && flutter test test/core/util/inbetriebnahme_test.dart` → FAIL (Datei fehlt).

- [ ] **Step 3: Implementieren** `lib/core/util/inbetriebnahme.dart`:

```dart
/// Aggregierter Inbetriebnahme-Fortschritt eines Stands (über seine Anlagen).
typedef Fortschritt = ({int inBetrieb, int total, bool komplett, String label});

/// [anlagen] = Liste aus (anzahl, inBetrieb). Zählt Anzahl-gewichtet.
Fortschritt inbetriebnahmeFortschritt(
    List<({int anzahl, bool inBetrieb})> anlagen) {
  var total = 0;
  var inBetrieb = 0;
  for (final a in anlagen) {
    total += a.anzahl;
    if (a.inBetrieb) inBetrieb += a.anzahl;
  }
  final komplett = total > 0 && inBetrieb == total;
  final label = total == 0
      ? '—'
      : komplett
          ? '✓ komplett'
          : '$inBetrieb/$total in Betrieb';
  return (inBetrieb: inBetrieb, total: total, komplett: komplett, label: label);
}
```

- [ ] **Step 4: Run — PASS** + Commit
```bash
git add sbs_projer_app/lib/core/util/inbetriebnahme.dart sbs_projer_app/test/core/util/inbetriebnahme_test.dart
git commit -m "feat(events): Inbetriebnahme-Fortschritt-Util inkl. Tests (E3, TDD)"
```

---

### Task 7: Anlagen id-basiert speichern (Formular-Erhalt der Inbetriebnahme)

**Files:** Modify `data/repositories/event_stand_anlage_repository.dart`, `presentation/screens/events/event_stand_form_screen.dart`

- [ ] **Step 1: `applyForStand` im Repository** (ersetzt `replaceForStand` für den Formular-Weg; erhält `inBetrieb` bestehender Anlagen):

```dart
  /// Gleicht die Anlagen eines Stands id-basiert ab: bestehende (per serverId)
  /// werden auf Typ/Anzahl aktualisiert (inBetrieb/inBetriebAm bleiben erhalten),
  /// neue eingefügt, entfernte gelöscht. [zeilen] in gewünschter Reihenfolge.
  static Future<void> applyForStand(
      String standId, List<({String? serverId, String typ, int anzahl})> zeilen) async {
    final bestehende = await getByStand(standId);
    final beiId = {
      for (final b in bestehende)
        if (b.serverId != null) b.serverId!: b,
    };
    final behalten = <String>{};
    for (var i = 0; i < zeilen.length; i++) {
      final z = zeilen[i];
      final vorhanden = z.serverId != null ? beiId[z.serverId] : null;
      if (vorhanden != null) {
        vorhanden
          ..typ = z.typ
          ..anzahl = z.anzahl
          ..sortierung = i;
        await save(vorhanden); // inBetrieb/inBetriebAm bleiben unveraendert
        behalten.add(z.serverId!);
      } else {
        final neu = EventStandAnlageLocal()
          ..standId = standId
          ..typ = z.typ
          ..anzahl = z.anzahl
          ..sortierung = i;
        await save(neu);
      }
    }
    for (final b in bestehende) {
      if (b.serverId != null && !behalten.contains(b.serverId)) {
        await delete(b.routeId);
      }
    }
  }
```
`replaceForStand` kann entfernt werden (nur der Formular-Save nutzte es; per Suche prüfen, dass keine andere Referenz bleibt).

- [ ] **Step 2: Formular auf serverIds + applyForStand umstellen**

In `event_stand_form_screen.dart`: paralleles Array `final List<String?> _anlagenServerIds = [];` ergänzen. In `_loadStand` beim Befüllen der Anlagen zusätzlich `_anlagenServerIds.add(a.serverId);`. In `_anlageHinzufuegen` `_anlagenServerIds.add(null);`. In `_anlageEntfernen(i)` `_anlagenServerIds.removeAt(i);`. In `_save` die Anlagen-Schleife ersetzen durch:

```dart
      final zeilen = <({String? serverId, String typ, int anzahl})>[
        for (var i = 0; i < _typen.length; i++)
          (serverId: _anlagenServerIds[i], typ: _typen[i], anzahl: _anzahl[i]),
      ];
      await EventStandAnlageRepository.applyForStand(stand.serverId!, zeilen);
```

- [ ] **Step 3: Analyze + Commit**
```bash
git add sbs_projer_app/lib/data/repositories/event_stand_anlage_repository.dart sbs_projer_app/lib/presentation/screens/events/event_stand_form_screen.dart
git commit -m "feat(events): Anlagen id-basiert speichern, Inbetriebnahme bleibt erhalten (E3)"
```

---

### Task 8: Inbetriebnahme-UI in der Stand-Karte

**Files:** Modify `presentation/screens/events/event_detail_screen.dart` (`_StandCard`)

- [ ] **Step 1: Fortschritts-Chip im Titel**

In `_StandCard.build`: `final fortschritt = inbetriebnahmeFortschritt(anlagen.map((a) => (anzahl: a.anzahl, inBetrieb: a.inBetrieb)).toList());`. Im `title`-Row rechts (neben/statt Standnummer-Chip) einen kleinen Chip mit `fortschritt.label` (grün wenn `komplett`, sonst grau). Import `inbetriebnahme.dart` ergänzen.

- [ ] **Step 2: Checkbox „in Betrieb" pro Anlagen-Zeile**

Die Anlagen-Zeilen-`Row` (die `...anlagen.map(...)`) um eine führende `Checkbox` erweitern:
```dart
                      Checkbox(
                        value: a.inBetrieb,
                        visualDensity: VisualDensity.compact,
                        onChanged: (v) async {
                          a
                            ..inBetrieb = v ?? false
                            ..inBetriebAm = (v ?? false) ? DateTime.now().toUtc() : null;
                          await EventStandAnlageRepository.save(a);
                          ref.invalidate(eventStandAnlagenProvider(stand.serverId!));
                        },
                      ),
```
(`_StandCard` ist `ConsumerWidget` → `ref` verfügbar; `a` ist `EventStandAnlageLocal`, mutierbar. Import Repository ergänzen.) Der Typ-Text + `×anzahl` bleiben in derselben Row rechts der Checkbox.

- [ ] **Step 3: Analyze + Commit**
```bash
git add sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): Inbetriebnahme-Checkbox + Fortschritt in Stand-Karte (E3)"
```

---

### Task 9: GPS-Standort erfassen

**Files:** Create `lib/services/gps/gps_service.dart`; Modify `event_detail_screen.dart` (`_StandCard` GPS-Aktion)

- [ ] **Step 1: GPS-Service** `lib/services/gps/gps_service.dart`:

```dart
import 'package:geolocator/geolocator.dart';

/// Dünne Hülle um geolocator für die einmalige Standort-Erfassung.
class GpsService {
  /// Holt die aktuelle Position; wirft mit verständlicher Meldung bei
  /// fehlender Berechtigung/deaktiviertem Dienst.
  static Future<Position> aktuellePosition() async {
    final aktiv = await Geolocator.isLocationServiceEnabled();
    if (!aktiv) {
      throw 'Standortdienst ist deaktiviert.';
    }
    var perm = await Geolocator.checkPermission();
    if (perm == LocationPermission.denied) {
      perm = await Geolocator.requestPermission();
    }
    if (perm == LocationPermission.denied ||
        perm == LocationPermission.deniedForever) {
      throw 'Kein Zugriff auf den Standort.';
    }
    return Geolocator.getCurrentPosition(
      locationSettings: const LocationSettings(accuracy: LocationAccuracy.high),
    );
  }
}
```
(Falls die geolocator-Version `getCurrentPosition` mit `desiredAccuracy:` statt `locationSettings:` erwartet: an die aufgelöste API anpassen — Kern bleibt `getCurrentPosition`.)

- [ ] **Step 2: GPS-Button in der Stand-Karte**

In `_StandCard` (z. B. als erstes `children`-Element der ExpansionTile oder in der `trailing`-Row ein `IconButton`): Aktion `_standortErfassen(context, ref, stand)`:
```dart
  Future<void> _standortErfassen(
      BuildContext context, WidgetRef ref, EventStandLocal stand) async {
    try {
      final pos = await GpsService.aktuellePosition();
      stand
        ..latitude = pos.latitude
        ..longitude = pos.longitude;
      await EventStandRepository.save(stand);
      ref.invalidate(eventStaendeProvider(stand.eventId));
      if (context.mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(content: Text('📍 Standort erfasst')),
        );
      }
    } catch (e) {
      if (context.mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text('Standort nicht möglich: $e')),
        );
      }
    }
  }
```
Anzeige: hat der Stand `latitude != null` → Zeile „📍 Standort erfasst" + Button „Neu erfassen"; sonst Button „📍 Standort erfassen". (`EventStandRepository` importieren; `stand.eventId` ist am Local vorhanden.)

- [ ] **Step 3: Analyze + Commit**
```bash
git add sbs_projer_app/lib/services/gps/gps_service.dart sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): GPS-Standort pro Stand erfassen (E3)"
```

---

### Task 10: Karten-Umschalter im Stände-Tab (swisstopo)

**Files:** Create `lib/presentation/screens/events/event_staende_map.dart`; Modify `event_detail_screen.dart` (`_StaendeTab`)

- [ ] **Step 1: Karten-Widget** `event_staende_map.dart` — `flutter_map` mit swisstopo-Luftbild + Marker je Stand mit Koordinaten:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_map/flutter_map.dart';
import 'package:latlong2/latlong.dart';
import 'package:sbs_projer_app/data/local/event_stand_local_export.dart';

/// Karte mit swisstopo-Luftbild und einem Marker je Stand (mit GPS).
/// Tap auf Marker ruft [onStandTap] mit dem Stand auf.
class EventStaendeMap extends StatelessWidget {
  final List<EventStandLocal> staende;
  final void Function(EventStandLocal) onStandTap;

  const EventStaendeMap(
      {super.key, required this.staende, required this.onStandTap});

  @override
  Widget build(BuildContext context) {
    final mitGps = staende
        .where((s) => s.latitude != null && s.longitude != null)
        .toList();
    if (mitGps.isEmpty) {
      return const Center(
        child: Padding(
          padding: EdgeInsets.all(24),
          child: Text(
            'Noch keine Stand-Standorte erfasst.\n'
            'Bei einem Stand «Standort erfassen» tippen.',
            textAlign: TextAlign.center,
          ),
        ),
      );
    }
    final punkte =
        mitGps.map((s) => LatLng(s.latitude!, s.longitude!)).toList();

    return FlutterMap(
      options: MapOptions(
        initialCameraFit: CameraFit.coordinates(
          coordinates: punkte,
          padding: const EdgeInsets.all(48),
          maxZoom: 18,
        ),
      ),
      children: [
        TileLayer(
          urlTemplate:
              'https://wmts.geo.admin.ch/1.0.0/ch.swisstopo.swissimage/default/current/3857/{z}/{x}/{y}.jpeg',
          userAgentPackageName: 'ch.sbsprojer.app',
          maxZoom: 19,
        ),
        MarkerLayer(
          markers: [
            for (final s in mitGps)
              Marker(
                point: LatLng(s.latitude!, s.longitude!),
                width: 40,
                height: 40,
                child: GestureDetector(
                  onTap: () => onStandTap(s),
                  child: const Icon(Icons.location_on,
                      color: Colors.red, size: 40),
                ),
              ),
          ],
        ),
        const RichAttributionWidget(
          attributions: [TextSourceAttribution('© swisstopo')],
        ),
      ],
    );
  }
}
```
(Hinweis: `CameraFit.coordinates` / `initialCameraFit` sind die flutter_map v7/v8-API. Weicht die aufgelöste Version ab, die entsprechende Kamera-API nutzen — Kernpunkte: `TileLayer.urlTemplate` swisstopo, `MarkerLayer` mit `GestureDetector`-Kindern, Anfangs-Ausschnitt auf die Punkte.)

- [ ] **Step 2: Umschalter im Stände-Tab**

`_StaendeTab` von `ConsumerWidget` auf `ConsumerStatefulWidget` umstellen (State `bool _karte = false`). Im Kopf neben „Aus Vorjahr übernehmen" ein `SegmentedButton`/zwei IconButtons **Liste ↔ Karte**. Bei `_karte == true`: statt der `ListView` das `EventStaendeMap(staende: staende, onStandTap: (s) => _standBearbeiten(context, ref, s))` rendern (nutzt dieselbe `staendeAsync.when`-Datenquelle). Tippt man einen Marker → Stand-Formular öffnen (bestehende `_standBearbeiten`).

- [ ] **Step 3: Analyze + Commit**
```bash
git add sbs_projer_app/lib/presentation/screens/events/event_staende_map.dart sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): Karten-Ansicht (swisstopo) im Staende-Tab (E3)"
```

---

### Task 11: Einsätze-Tab + Formular

**Files:** Create `lib/presentation/screens/events/event_einsatz_form_screen.dart`; Modify `event_detail_screen.dart` (TabController length 4, TabBar, `_buildFab`, neuer `_EinsaetzeTab`)

- [ ] **Step 1: TabController + TabBar + FAB auf 4 Tabs**

`initState`: `TabController(length: 4, …)`. TabBar-Tabs: `Kontakte`, `Stände`, `Einsätze`, `Dokumente`. `_buildFab`: `case 2` → FAB „Einsatz erfassen" (`_einsatzHinzufuegen(eventServerId)`), `case 3` → das bisherige Dokument-FAB (Index verschiebt sich!). TabBarView entsprechend: Kontakte, Stände, **_EinsaetzeTab**, Dokumente.

- [ ] **Step 2: `_EinsaetzeTab`** (ConsumerWidget): `ref.watch(eventEinsaetzeProvider(event.serverId!))`. Liste (neueste zuerst): pro Einsatz `Card` mit Zeitpunkt `dd.MM. HH:mm`, Beschreibung (fett), Material-Zeile (falls vorhanden), optional Stand-Name-Chip (Stand-Name via `eventStaendeProvider` nachschlagen). Tap → `EventEinsatzFormScreen(eventId, einsatzId)`. Löschen (Icon/Swipe, Dialog). Leer-Zustand „Noch keine Einsätze".

- [ ] **Step 3: `EventEinsatzFormScreen({required eventId, einsatzId})`** — `ConsumerStatefulWidget`, minimal:
- Beschreibung (`TextFormField`, Pflicht, maxLines 3)
- Material (`TextFormField`, optional)
- Stand (Dropdown aus `eventStaendeProvider(eventId)` + Eintrag „— (kein Stand)")
- Zeitpunkt: Default `DateTime.now()`, über Datum- + Zeit-Picker editierbar (Anzeige `dd.MM.yyyy HH:mm`)
- Speichern: `EventEinsatzLocal()..eventId=eventId..standId=…..zeitpunkt=…..beschreibung=…..material=…` → `EventEinsatzRepository.save` → `ref.invalidate(eventEinsaetzeProvider(eventId))` → `pop`. Bearbeiten: Einsatz laden (per `getByEvent` + routeId-Filter, Muster Stand-Formular), Felder füllen.

- [ ] **Step 4: Analyze + volle Tests + Commit**

Run: `flutter analyze && flutter test` → sauber, alle grün.
```bash
git add sbs_projer_app/lib/presentation/screens/events/event_einsatz_form_screen.dart sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): Einsaetze-Tab + Pikett-Einsatz-Formular (E3)"
```

---

### Task 12: Gesamtverifikation, visueller Test, Deploy, Doku

- [ ] **Step 1:** `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && flutter analyze && flutter test` — 0 neue errors/warnings, alle Tests grün.

- [ ] **Step 2: Visueller Browser-Test (PFLICHT):** lokaler Build (`--pwa-strategy=none`, ohne base-href) + Server. Checkliste:
1. Stand aufklappen → Checkbox „in Betrieb" pro Anlage; abhaken → Fortschritts-Chip am Stand ändert sich („5/10", „✓ komplett")
2. Stand bearbeiten (Anzahl ändern, Anlage hinzufügen/entfernen) → **Häkchen bestehender Anlagen bleiben erhalten**
3. „Standort erfassen" (localhost erlaubt GPS) → Snackbar „Standort erfasst"
4. Stände-Tab Umschalter → **Karte**: swisstopo-Luftbild lädt, Marker am erfassten Stand, Tap → Stand-Formular
5. Einsätze-Tab: Einsatz anlegen (Beschreibung + Material + optional Stand + Zeit), erscheint neueste zuerst; bearbeiten; löschen
6. Kontakte + Dokumente-Tab unverändert funktionsfähig (FAB-Index-Verschiebung geprüft)

- [ ] **Step 3: Version** `pubspec.yaml` → `0.19.0+482`.

- [ ] **Step 4: Deploy** gemäss CLAUDE.md (main committen+pushen, Build `--base-href "/sbs-projer-dev/"` `--pwa-strategy=none`, Cache-Bust, gh-pages, zurück main).

- [ ] **Step 5: Doku** ToDo.md (E3 erledigt, E4 als nächste) + Projekt.md-Header. Commit + Push.
