# Events-Modul Phase E2 — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Dokument-Ablage + Stände + Schankanlagen pro Event-Jahr gemäss Spec `docs/superpowers/specs/2026-07-10-events-e2-design.md`. Event-Detail wird auf Tabs (Kontakte | Stände | Dokumente) umgebaut.

**Architecture:** Drei neue gesyncte Entities (`event_dokumente`, `event_staende`, `event_stand_anlagen`) exakt nach der E1-Event-Vertikale (die committeten E1-Dateien sind die strukturelle Kopiervorlage). Dokumente liegen im neuen privaten Storage-Bucket `event-dokumente`. Client-seitige UUIDs (`serverId ??= Uuid().v4()`), Native-Delete IMMER serverseitig + lokal (E1-Review W1).

**Tech Stack:** Flutter/Dart, Riverpod, Isar (Conditional Exports), Supabase (MCP `apply_migration`/`execute_sql`, Project `pltbaqqwpnmdajwgnhpd`, Storage), file_picker, url_launcher, uuid.

**Projektregeln:**
- Shell Git Bash, `export PATH="$PATH:/c/flutter/bin"`, App-Verzeichnis `sbs_projer_app`.
- Local-Imports über `*_export.dart` (Ausnahme: `isar_service.dart` importiert native Locals direkt).
- KEINE `?ausdruck`-Collection-Elemente (bricht build_runner). Immer `if (x != null) …`.
- `.g.dart` ist gitignored — nie stagen.
- Kommentare/Labels Deutsch. Nach jedem Task `flutter analyze` ohne neue errors/warnings (Baseline 47 infos), dann committen.
- **Strukturelle Kopiervorlage E1** (vorher lesen): `data/models/event.dart`, `data/local/event_local.dart` (+ `web/event_local_web.dart`, `_export.dart`), `data/mappers/event_mapper.dart`, `data/repositories/event_repository.dart`, `presentation/providers/event_providers.dart`, `services/sync/sync_service.dart` (`_syncEvents`).

---

### Task 1: Migration 120 — Tabellen + Storage-Bucket

**Files:** Create `Datenbank/migrations/120_events_e2.sql`

- [ ] **Step 1: Migration schreiben**

```sql
-- ============================================================
-- Migration 120: Events E2 — Dokumente, Staende, Stand-Anlagen
-- Projekt: SBS Projer App
-- Stand: 10.07.2026
-- ============================================================

-- ── event_dokumente ──
CREATE TABLE event_dokumente (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  event_id uuid NOT NULL REFERENCES events(id) ON DELETE CASCADE,
  bezeichnung text NOT NULL,
  datei_pfad text NOT NULL,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
ALTER TABLE event_dokumente ENABLE ROW LEVEL SECURITY;
CREATE POLICY event_dokumente_user_policy ON event_dokumente
  FOR ALL USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
CREATE TRIGGER update_event_dokumente_updated_at BEFORE UPDATE ON event_dokumente
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE INDEX idx_event_dokumente_event ON event_dokumente(user_id, event_id);

-- ── event_staende ──
CREATE TABLE event_staende (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  event_id uuid NOT NULL REFERENCES events(id) ON DELETE CASCADE,
  name text NOT NULL,
  standnummer text,
  sortierung int NOT NULL DEFAULT 0,
  notizen text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
ALTER TABLE event_staende ENABLE ROW LEVEL SECURITY;
CREATE POLICY event_staende_user_policy ON event_staende
  FOR ALL USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
CREATE TRIGGER update_event_staende_updated_at BEFORE UPDATE ON event_staende
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE INDEX idx_event_staende_event ON event_staende(user_id, event_id);

-- ── event_stand_anlagen ──
CREATE TABLE event_stand_anlagen (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  stand_id uuid NOT NULL REFERENCES event_staende(id) ON DELETE CASCADE,
  typ text NOT NULL CHECK (typ IN (
    'oberthekengeraet', 'hollandbuffet', 'ausschankwagen', 'sonstige'
  )),
  bezeichnung text,
  anzahl int NOT NULL DEFAULT 1,
  sortierung int NOT NULL DEFAULT 0,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
ALTER TABLE event_stand_anlagen ENABLE ROW LEVEL SECURITY;
CREATE POLICY event_stand_anlagen_user_policy ON event_stand_anlagen
  FOR ALL USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
CREATE TRIGGER update_event_stand_anlagen_updated_at BEFORE UPDATE ON event_stand_anlagen
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE INDEX idx_event_stand_anlagen_stand ON event_stand_anlagen(user_id, stand_id);

-- ── Storage-Bucket event-dokumente (privat, 20MB, nur PDF) ──
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES ('event-dokumente', 'event-dokumente', false, 20971520, ARRAY['application/pdf'])
ON CONFLICT (id) DO NOTHING;

CREATE POLICY "Users upload own event docs" ON storage.objects FOR INSERT TO authenticated
  WITH CHECK (bucket_id = 'event-dokumente' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Users read own event docs" ON storage.objects FOR SELECT TO authenticated
  USING (bucket_id = 'event-dokumente' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Users update own event docs" ON storage.objects FOR UPDATE TO authenticated
  USING (bucket_id = 'event-dokumente' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Users delete own event docs" ON storage.objects FOR DELETE TO authenticated
  USING (bucket_id = 'event-dokumente' AND (storage.foldername(name))[1] = auth.uid()::text);
```

- [ ] **Step 2: Anwenden + verifizieren** (Supabase MCP `apply_migration`, name `120_events_e2`). Danach `execute_sql`: `SELECT table_name FROM information_schema.tables WHERE table_name LIKE 'event_%';` → enthält dokumente/staende/stand_anlagen; und `SELECT id FROM storage.buckets WHERE id='event-dokumente';` → 1 Zeile.

- [ ] **Step 3: Commit**
```bash
git add Datenbank/migrations/120_events_e2.sql
git commit -m "feat(events): Migration 120 Dokumente/Staende/Anlagen + Storage-Bucket (E2)"
```

---

### Task 2: DTOs

**Files:** Create `data/models/event_dokument.dart`, `event_stand.dart`, `event_stand_anlage.dart`

- [ ] **Step 1: DTOs erstellen** — exakt nach dem Muster `data/models/event.dart` (fromJson mit DateTime-Guards, toJson mit `id` nur wenn gesetzt bzw. immer inkl. Feldern; `created_at`/`updated_at` NICHT in toJson). Felder:

`EventDokument`: `id, userId(user_id), eventId(event_id), bezeichnung, dateiPfad(datei_pfad), createdAt, updatedAt`.

`EventStand`: `id, userId, eventId, name, standnummer, sortierung(int, default 0), notizen, createdAt, updatedAt`. Zusätzlich statischer Helfer:
```dart
  /// Kurz-Zusammenfassung der Anlagen für die Stand-Karte, z. B. "7× OT · 3× Hollandbuffet".
  static String anlagenText(List<({String typ, int anzahl})> anlagen) {
    if (anlagen.isEmpty) return 'Keine Anlagen';
    final zusammen = <String, int>{};
    for (final a in anlagen) {
      zusammen[a.typ] = (zusammen[a.typ] ?? 0) + a.anzahl;
    }
    return zusammen.entries
        .map((e) => '${e.value}× ${EventStandAnlage.typKurz(e.key)}')
        .join(' · ');
  }
```

`EventStandAnlage`: `id, userId, standId(stand_id), typ, bezeichnung, anzahl(int, default 1), sortierung(int, default 0), createdAt, updatedAt`. Plus:
```dart
  static const typen = ['oberthekengeraet', 'hollandbuffet', 'ausschankwagen', 'sonstige'];
  static String typLabel(String typ) => switch (typ) {
        'oberthekengeraet' => 'Oberthekengerät',
        'hollandbuffet' => 'Hollandbuffet',
        'ausschankwagen' => 'Ausschankwagen',
        'sonstige' => 'Sonstige',
        _ => typ,
      };
  static String typKurz(String typ) => switch (typ) {
        'oberthekengeraet' => 'OT',
        'hollandbuffet' => 'Hollandbuffet',
        'ausschankwagen' => 'Ausschankwagen',
        'sonstige' => 'Sonstige',
        _ => typ,
      };
```
(`EventStand.anlagenText` referenziert `EventStandAnlage.typKurz` → beide im selben Import erreichbar; ggf. `event_stand.dart` importiert `event_stand_anlage.dart`.)

- [ ] **Step 2: Analyze + Commit**
```bash
git add sbs_projer_app/lib/data/models/event_dokument.dart sbs_projer_app/lib/data/models/event_stand.dart sbs_projer_app/lib/data/models/event_stand_anlage.dart
git commit -m "feat(events): DTOs EventDokument/EventStand/EventStandAnlage (E2)"
```

---

### Task 3: Isar-Locals, Web-Stubs, Exports, IsarService

**Files:** je Entity `data/local/<name>_local.dart`, `web/<name>_local_web.dart`, `<name>_local_export.dart`; Modify `services/storage/isar_service.dart` + `isar_service_web.dart`.

- [ ] **Step 1: Isar-Locals** — Struktur exakt wie `event_local.dart` (Id, `@ignore routeId`, `@Index() serverId/isSynced`, `lastModifiedAt`), Felder wie DTO ohne created/updated-Semantikänderung. Bei `EventStandAnlageLocal`: `@Index() late String standId;`. Bei `EventStandLocal`/`EventDokumentLocal`: `@Index() late String eventId;`. Anzahl/sortierung als `int` (mit Defaults, nicht `late`).

- [ ] **Step 2: Web-Stubs** — Plain-Klassen wie `web/event_local_web.dart` (kein Isar, kein `late`, Default-Werte; `routeId => serverId!`).

- [ ] **Step 3: Exports** — je `export '<name>_local.dart' if (dart.library.html) 'web/<name>_local_web.dart';`

- [ ] **Step 4: IsarService** — Imports der drei Locals ergänzen, `EventDokumentLocalSchema`, `EventStandLocalSchema`, `EventStandAnlageLocalSchema` in die Schema-Liste. Methoden nach dem EventKontakt-Block:
```dart
  // ─── EventDokument ───
  static Future<List<EventDokumentLocal>> eventDokumentFindByEvent(String eventId) =>
      instance.eventDokumentLocals.filter().eventIdEqualTo(eventId).findAll();
  static Future<EventDokumentLocal?> eventDokumentGet(int id) =>
      instance.eventDokumentLocals.get(id);
  static Future<EventDokumentLocal?> eventDokumentFindByServerId(String serverId) =>
      instance.eventDokumentLocals.filter().serverIdEqualTo(serverId).findFirst();
  static Future<void> eventDokumentPut(EventDokumentLocal d) =>
      instance.writeTxn(() => instance.eventDokumentLocals.put(d));
  static Future<void> eventDokumentDelete(int id) =>
      instance.writeTxn(() => instance.eventDokumentLocals.delete(id));

  // ─── EventStand ───
  static Future<List<EventStandLocal>> eventStandFindByEvent(String eventId) =>
      instance.eventStandLocals.filter().eventIdEqualTo(eventId).findAll();
  static Future<EventStandLocal?> eventStandGet(int id) =>
      instance.eventStandLocals.get(id);
  static Future<EventStandLocal?> eventStandFindByServerId(String serverId) =>
      instance.eventStandLocals.filter().serverIdEqualTo(serverId).findFirst();
  static Future<void> eventStandPut(EventStandLocal s) =>
      instance.writeTxn(() => instance.eventStandLocals.put(s));
  static Future<void> eventStandDelete(int id) =>
      instance.writeTxn(() => instance.eventStandLocals.delete(id));

  // ─── EventStandAnlage ───
  static Future<List<EventStandAnlageLocal>> eventStandAnlageFindByStand(String standId) =>
      instance.eventStandAnlageLocals.filter().standIdEqualTo(standId).findAll();
  static Future<EventStandAnlageLocal?> eventStandAnlageGet(int id) =>
      instance.eventStandAnlageLocals.get(id);
  static Future<EventStandAnlageLocal?> eventStandAnlageFindByServerId(String serverId) =>
      instance.eventStandAnlageLocals.filter().serverIdEqualTo(serverId).findFirst();
  static Future<void> eventStandAnlagePut(EventStandAnlageLocal a) =>
      instance.writeTxn(() => instance.eventStandAnlageLocals.put(a));
  static Future<void> eventStandAnlageDelete(int id) =>
      instance.writeTxn(() => instance.eventStandAnlageLocals.delete(id));
```
In `isar_service_web.dart` die 15 analogen `UnsupportedError`-Stubs ergänzen.

- [ ] **Step 5: build_runner + Analyze** — `dart run build_runner build --delete-conflicting-outputs` (Timeout 600000) → „Succeeded"; `flutter analyze` ohne neue Findings.

- [ ] **Step 6: Commit** (`.g.dart` NICHT stagen)
```bash
git add sbs_projer_app/lib/data/local/event_dokument_local.dart sbs_projer_app/lib/data/local/event_stand_local.dart sbs_projer_app/lib/data/local/event_stand_anlage_local.dart sbs_projer_app/lib/data/local/event_dokument_local_export.dart sbs_projer_app/lib/data/local/event_stand_local_export.dart sbs_projer_app/lib/data/local/event_stand_anlage_local_export.dart sbs_projer_app/lib/data/local/web/event_dokument_local_web.dart sbs_projer_app/lib/data/local/web/event_stand_local_web.dart sbs_projer_app/lib/data/local/web/event_stand_anlage_local_web.dart sbs_projer_app/lib/services/storage/isar_service.dart sbs_projer_app/lib/services/storage/isar_service_web.dart
git commit -m "feat(events): Isar-Locals + IsarService fuer Dokumente/Staende/Anlagen (E2)"
```

---

### Task 4: Storage-Service für Event-Dokumente

**Files:** Create `services/storage/event_dokument_storage.dart`

- [ ] **Step 1: Service schreiben** (Muster `protokoll_foto_storage.dart`; Pfad MUSS mit userId beginnen — RLS prüft `foldername[1] = auth.uid()`):

```dart
import 'dart:typed_data';
import 'package:uuid/uuid.dart';
import 'package:sbs_projer_app/services/supabase/supabase_service.dart';

/// Storage-Zugriff für Event-Dokumente (PDF) im privaten Bucket 'event-dokumente'.
class EventDokumentStorage {
  static const _bucket = 'event-dokumente';
  static String get _userId => SupabaseService.dataUserId;

  /// Lädt ein PDF hoch und gibt den gespeicherten Pfad zurück.
  /// Pfad: <userId>/<eventId>/<uuid>.pdf (erste Ebene = userId für RLS).
  static Future<String> upload(String eventId, Uint8List pdfBytes) async {
    final pfad = '$_userId/$eventId/${const Uuid().v4()}.pdf';
    await SupabaseService.client.storage.from(_bucket).uploadBinary(
          pfad,
          pdfBytes,
          fileOptions: const FileOptions(contentType: 'application/pdf', upsert: true),
        );
    return pfad;
  }

  /// Signierte URL (1h gültig) zum Öffnen im PDF-Viewer.
  static Future<String> getSignedUrl(String pfad) =>
      SupabaseService.client.storage.from(_bucket).createSignedUrl(pfad, 3600);

  /// Entfernt die Datei aus dem Bucket (Fehler werden geschluckt — Datensatz-Löschung hat Vorrang).
  static Future<void> delete(String pfad) async {
    try {
      await SupabaseService.client.storage.from(_bucket).remove([pfad]);
    } catch (_) {}
  }
}
```
(Falls `FileOptions` nicht aufgelöst wird: Import wie in `protokoll_foto_storage.dart` übernehmen.)

- [ ] **Step 2: Analyze + Commit**
```bash
git add sbs_projer_app/lib/services/storage/event_dokument_storage.dart
git commit -m "feat(events): Storage-Service fuer Event-Dokumente (E2)"
```

---

### Task 5: Mapper, Repositories, Provider, Stände-Übernahme (+ Test)

**Files:** je Entity Mapper + Repository; `presentation/providers/event_providers.dart` erweitern; Test `test/data/event_staende_test.dart`.

- [ ] **Step 1: Mapper** — je `fromDto(dto, {existing})` + `toJson(local)` exakt nach `event_mapper.dart` (serverId→id, isSynced=true, lastModifiedAt-Fallback; toJson nur Sync-Felder, `id` wenn serverId gesetzt).

- [ ] **Step 2: EventDokumentRepository** — nach `event_repository.dart`-Muster (Import `isar_service_export.dart`, `_userId`):
  - `getByEvent(String eventId)`: Web `from('event_dokumente').select().eq('user_id',_userId).eq('event_id',eventId).order('created_at')`; Native `eventDokumentFindByEvent`.
  - `save(EventDokumentLocal d)`: `d.userId=_userId; d.serverId ??= Uuid().v4();` Web upsert; Native `isSynced=false; lastModifiedAt=now(); eventDokumentPut`.
  - `delete(String id)`: Web delete eq id; Native serverseitig zuerst (via `eventDokumentGet` → serverId), dann `eventDokumentDelete` (E1-W1-Muster).

- [ ] **Step 3: EventStandAnlageRepository** — analog, plus Replace-Helfer:
```dart
  /// Ersetzt alle Anlagen eines Stands durch [neu] (löscht bestehende, legt neue an).
  static Future<void> replaceForStand(
      String standId, List<EventStandAnlageLocal> neu) async {
    final alt = await getByStand(standId);
    for (final a in alt) {
      await delete(a.routeId);
    }
    for (var i = 0; i < neu.length; i++) {
      final a = neu[i]
        ..standId = standId
        ..sortierung = i;
      await save(a);
    }
  }
```
`getByStand(standId)`: Web `.eq('stand_id',standId).order('sortierung')`; Native `eventStandAnlageFindByStand`. `delete` serverseitig+lokal.

- [ ] **Step 4: EventStandRepository** — nach `event_repository.dart`, plus:
  - `getByEvent(eventId)`: Web `.eq('event_id',eventId).order('sortierung').order('name')`; Native `eventStandFindByEvent`.
  - `delete(String id)`: serverseitig+lokal; Anlagen räumt DB-CASCADE ab (Web); Native zusätzlich `EventStandAnlageRepository.getByStand + delete` vor dem Stand-Delete.
  - **Pure Merge-Funktion** (testbar):
```dart
  /// Stände aus [quelle], deren name (case-insensitive, getrimmt) in [ziel] fehlt.
  static List<EventStandLocal> fehlendeStaende(
      List<EventStandLocal> quelle, List<EventStandLocal> ziel) {
    String norm(String s) => s.trim().toLowerCase();
    final vorhanden = ziel.map((z) => norm(z.name)).toSet();
    return quelle.where((q) => !vorhanden.contains(norm(q.name))).toList();
  }
```
  - `uebernehmeVon(String vonEventId, String zuEventId)`: lädt Quell-/Ziel-Stände, `fehlendeStaende`, legt je Stand einen neuen an (name/standnummer/sortierung/notizen) und kopiert dessen Anlagen (via `EventStandAnlageRepository.getByStand(quelleStand.serverId!)` → neue Anlagen mit neuem standId). Rückgabe: Anzahl übernommener Stände.

- [ ] **Step 5: Provider** in `event_providers.dart`:
```dart
final eventDokumenteProvider =
    FutureProvider.family<List<EventDokumentLocal>, String>((ref, eventId) async {
  return EventDokumentRepository.getByEvent(eventId);
});
final eventStaendeProvider =
    FutureProvider.family<List<EventStandLocal>, String>((ref, eventId) async {
  return EventStandRepository.getByEvent(eventId);
});
final eventStandAnlagenProvider =
    FutureProvider.family<List<EventStandAnlageLocal>, String>((ref, standId) async {
  return EventStandAnlageRepository.getByStand(standId);
});
```

- [ ] **Step 6: Test (TDD-Reihenfolge)** `test/data/event_staende_test.dart` — erst schreiben, Fail sehen, dann Merge fertigstellen:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:sbs_projer_app/data/local/event_stand_local_export.dart';
import 'package:sbs_projer_app/data/repositories/event_stand_repository.dart';

EventStandLocal _s(String name) => EventStandLocal()..userId='t'..eventId='e'..name=name;

void main() {
  group('fehlendeStaende', () {
    test('leeres Ziel: alle', () {
      expect(EventStandRepository.fehlendeStaende([_s('VIP'), _s('Kiosk')], []).length, 2);
    });
    test('gleicher Name (case-insensitive, getrimmt) uebersprungen', () {
      final neu = EventStandRepository.fehlendeStaende(
          [_s('VIP'), _s('Kiosk')], [_s(' vip ')]);
      expect(neu.length, 1);
      expect(neu.first.name, 'Kiosk');
    });
  });
}
```
Und ein Test für `EventStand.anlagenText` (Aggregation "2× OT · 1× Hollandbuffet") in `test/core/...` oder derselben Datei.

- [ ] **Step 7: Analyze + Tests + Commit**
```bash
git add sbs_projer_app/lib/data/mappers/event_dokument_mapper.dart sbs_projer_app/lib/data/mappers/event_stand_mapper.dart sbs_projer_app/lib/data/mappers/event_stand_anlage_mapper.dart sbs_projer_app/lib/data/repositories/event_dokument_repository.dart sbs_projer_app/lib/data/repositories/event_stand_repository.dart sbs_projer_app/lib/data/repositories/event_stand_anlage_repository.dart sbs_projer_app/lib/presentation/providers/event_providers.dart sbs_projer_app/test/data/event_staende_test.dart
git commit -m "feat(events): Mapper/Repos/Provider + Staende-Uebernahme inkl. Test (E2)"
```

---

### Task 6: Sync-Integration

**Files:** Modify `services/sync/sync_service.dart`

- [ ] **Step 1:** Drei Sync-Methoden `_syncEventDokumente`, `_syncEventStaende`, `_syncEventStandAnlagen` exakt nach `_syncEventKontakte` (Tabellen `event_dokumente`/`event_staende`/`event_stand_anlagen`, passende Mapper, Meta-Keys gleichnamig, LWW-Muster identisch).

- [ ] **Step 2:** In Tier-Struktur einhängen (Tiers Z. 128–159): `_syncEventDokumente` + `_syncEventStaende` in **Tier 3** (neben `_syncEventKontakte`, brauchen events); `_syncEventStandAnlagen` in **Tier 4** (braucht staende). Vorher die Tier-Listen lesen.

- [ ] **Step 3: Analyze + Commit**
```bash
git add sbs_projer_app/lib/services/sync/sync_service.dart
git commit -m "feat(events): Sync-Integration Dokumente/Staende/Anlagen (E2)"
```

---

### Task 7: Event-Detail auf Tabs umbauen (Kontakte | Stände | Dokumente)

**Files:** Modify `presentation/screens/events/event_detail_screen.dart`

Ziel: Kopf-Card (`_KopfCard`, Z. 397) bleibt oben; darunter `TabBar` (3 Tabs) + `TabBarView`. Die bestehende Kontaktliste + Zuordnen-Sheet + WhatsApp/Anruf wandern **unverändert** in den ersten Tab. Menü (Vorjahr/Löschen) bleibt in der AppBar. Der FAB wird tab-abhängig.

- [ ] **Step 1:** State-Klasse auf `SingleTickerProviderStateMixin`, `TabController(length: 3)` in `initState`, Listener `setState`. Scaffold-Body: `Column([ _KopfCard(...), TabBar(tabs: Kontakte/Stände/Dokumente), Expanded(TabBarView([...])) ])`. Bestehenden Kontakt-Listen-Aufbau (Z. 274–392) in ein privates Widget `_KontakteTab` extrahieren (gleicher Code, gleiche Provider/Invalidierungen).
- [ ] **Step 2:** FAB je Tab: Index 0 „Kontakt zuordnen" (bisher), 1 „Stand hinzufügen" (→ Task 8), 2 „Dokument hochladen" (→ Task 9). In diesem Task Tabs 1/2 mit Platzhalter-Widgets `_StaendeTab`/`_DokumenteTab` (einfaches Scaffold-Center „folgt") füllen, FAB-Aktionen für 1/2 vorerst leer/kein FAB — werden in Task 8/9 ersetzt.
- [ ] **Step 3:** Analyze — Kontakte-Tab muss unverändert funktionieren. Commit:
```bash
git add sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): Event-Detail auf Tabs (Kontakte|Staende|Dokumente) (E2)"
```

---

### Task 8: Stände-Tab + Stand-Formular

**Files:** Create `presentation/screens/events/event_stand_form_screen.dart`; Modify `event_detail_screen.dart` (`_StaendeTab` + FAB-Aktion).

- [ ] **Step 1: `_StaendeTab`** (in event_detail_screen.dart): `ref.watch(eventStaendeProvider(event.serverId!))`. Pro Stand eine `ExpansionTile`/Card: Titel = Name (+ Standnummer-Chip), Untertitel = `EventStand.anlagenText(...)` aus `ref.watch(eventStandAnlagenProvider(stand.serverId!))`; aufgeklappt: Anlagen-Zeilen (`typLabel` + Bezeichnung + „×anzahl"), Notizen; Trailing: Stift → Stand-Form (bearbeiten), Overflow/Swipe → Löschen (Dialog; `EventStandRepository.delete` + invalidate). Leer-Zustand „Noch keine Stände". Menü-Eintrag (in AppBar-PopupMenu oder Tab-Kopf) „Stände aus Vorjahr übernehmen" → `EventStandRepository.uebernehmeVon(vorjahr.serverId!, event.serverId!)` + Snackbar „X Stände übernommen" + invalidate `eventStaendeProvider`.
- [ ] **Step 2: `EventStandFormScreen({required eventId, standId})`** — `ConsumerStatefulWidget`. Felder: Name (Pflicht), Standnummer, Notizen. **Dynamische Anlagen-Zeilen** (Muster Montage-Material/Ferien): Parallel-Arrays `List<String> _typen`, `List<TextEditingController> _bezCtrl`, `List<int> _anzahl` + „+ Anlage"-Button, je Zeile Typ-`DropdownButtonFormField` (`EventStandAnlage.typen`/`typLabel`, Default `oberthekengeraet`), Bezeichnung-Feld, Anzahl (Zahlfeld/Stepper min 1), Zeile entfernen (Icon). Bearbeiten-Modus: Stand + Anlagen laden.
- [ ] **Step 3: Speichern:** Stand anlegen/aktualisieren (`EventStandRepository.save`), dann `EventStandAnlageRepository.replaceForStand(stand.serverId!, anlagenLocals)`. `ref.invalidate(eventStaendeProvider(eventId))` + `eventStandAnlagenProvider(stand.serverId!)`; `context.pop()`. FAB-Aktion in `_StaendeTab`: `await context.push` auf ein Formular-Routing ODER direkt `Navigator.push(EventStandFormScreen(eventId: event.serverId!))`, danach invalidate.
- [ ] **Step 4: Analyze + Commit**
```bash
git add sbs_projer_app/lib/presentation/screens/events/event_stand_form_screen.dart sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): Staende-Tab + Stand-Formular mit dynamischen Anlagen (E2)"
```

---

### Task 9: Dokumente-Tab + Upload/Öffnen/Löschen

**Files:** Modify `event_detail_screen.dart` (`_DokumenteTab` + FAB-Aktion)

- [ ] **Step 1: `_DokumenteTab`**: `ref.watch(eventDokumenteProvider(event.serverId!))`. Liste: Bezeichnung + Upload-Datum (createdAt), Leericon `Icons.picture_as_pdf`; Tap → `EventDokumentStorage.getSignedUrl(pfad)` dann `launchUrl(Uri.parse(url), mode: LaunchMode.externalApplication)`; Trailing Löschen (Dialog → `EventDokumentStorage.delete(pfad)` dann `EventDokumentRepository.delete(routeId)` + invalidate). Leer-Zustand „Noch keine Dokumente".
- [ ] **Step 2: FAB „Dokument hochladen"** (Tab-Index 2): `FilePicker.platform.pickFiles(type: FileType.custom, allowedExtensions: ['pdf'], withData: true)` (Muster eingangsrechnung_upload_screen). Bytes prüfen. Bezeichnung-Dialog (`TextField`, Vorschlag = Dateiname ohne `.pdf`). Dann `final pfad = await EventDokumentStorage.upload(event.serverId!, bytes);` und `EventDokumentRepository.save(EventDokumentLocal()..eventId=event.serverId!..bezeichnung=bez..dateiPfad=pfad)`; Fortschritts-/Fehler-Snackbar; invalidate `eventDokumenteProvider`.
- [ ] **Step 3: Analyze + volle Tests** (`flutter analyze && flutter test`) + Commit
```bash
git add sbs_projer_app/lib/presentation/screens/events/event_detail_screen.dart
git commit -m "feat(events): Dokumente-Tab mit PDF-Upload/Ansicht (E2)"
```

---

### Task 10: Event-Formular — Checkbox „Stände aus Vorjahr übernehmen"

**Files:** Modify `presentation/screens/events/event_form_screen.dart`

- [ ] **Step 1:** Zweite Checkbox `_uebernehmeVorjahrStaende` (State analog `_uebernehmeVorjahr`, Z. 37), sichtbar zusammen mit der Kontakte-Checkbox, sobald ein Vorjahres-Event existiert; Label „Stände aus ‹Name Jahr› übernehmen"; Default angehakt. Bei Betrieb-/Jahr-Wechsel im selben `_pruefeVorjahr()`-Flow mitsetzen.
- [ ] **Step 2:** Nach `EventRepository.save` und der Kontakte-Übernahme: wenn `!_isEdit && _uebernehmeVorjahrStaende && _vorjahrEvent != null` → `staende = await EventStandRepository.uebernehmeVon(_vorjahrEvent!.serverId!, e.serverId!)`. Kombinierte Snackbar (Singular/Plural, wie E1): z. B. „3 Kontakte, 12 Stände übernommen" (nur Teile zeigen, die > 0 bzw. beide immer nennen — konsistent formulieren).
- [ ] **Step 3: Analyze + Commit**
```bash
git add sbs_projer_app/lib/presentation/screens/events/event_form_screen.dart
git commit -m "feat(events): Vorjahres-Uebernahme auch fuer Staende (E2)"
```

---

### Task 11: Gesamtverifikation, visueller Test, Deploy, Doku

- [ ] **Step 1:** `export PATH="$PATH:/c/flutter/bin" && cd sbs_projer_app && flutter analyze && flutter test` — 0 neue errors/warnings, alle Tests grün.
- [ ] **Step 2: Visueller Browser-Test (PFLICHT):** lokaler Build (`--pwa-strategy=none`, ohne base-href) + Server. Checkliste: Event-Detail zeigt 3 Tabs; Kontakte-Tab unverändert; Stand anlegen mit mehreren Anlagen-Zeilen (OT/Hollandbuffet/Ausschankwagen), Zusammenfassung stimmt; Stand bearbeiten (Anlage ändern/entfernen → replace korrekt); Stand löschen; **Dokument hochladen mit echtem `00_Event/Lageplan 2024.pdf`**, öffnen (PDF-Viewer), löschen; zweites Event-Jahr anlegen → Kontakte + Stände übernommen; Kombi-Snackbar korrekt.
- [ ] **Step 3: Version** `pubspec.yaml` → `0.18.0+480`.
- [ ] **Step 4: Deploy** gemäss CLAUDE.md (main committen+pushen, Build `--base-href "/sbs-projer-dev/"` `--pwa-strategy=none`, Cache-Bust, gh-pages, zurück main).
- [ ] **Step 5: Doku** ToDo.md (E2 erledigt, E3/E4 als nächste) + Projekt.md-Header. Commit + Push.
